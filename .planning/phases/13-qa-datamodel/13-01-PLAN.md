---
phase: 13-qa-datamodel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/00002_qa_schema.sql
  - docs/database-schema.md
  - docs/CHANGELOG.md
autonomous: true

must_haves:
  truths:
    - "questions, answers, en answer_replies tabellen bestaan in de database"
    - "Anonieme gebruikers kunnen vragen, antwoorden en reacties lezen (public SELECT)"
    - "Alleen ingelogde gebruikers kunnen vragen, antwoorden en reacties plaatsen"
    - "Gebruikers kunnen alleen hun eigen rijen updaten of verwijderen"
    - "Cascade deletes zorgen dat antwoorden verdwijnen als een vraag verwijderd wordt"
    - "docs/database-schema.md beschrijft het Q&A schema"
  artifacts:
    - path: "supabase/migrations/00002_qa_schema.sql"
      provides: "SQL migration met alle 3 tabellen, RLS, triggers, indexes"
      contains: "create table public.questions"
    - path: "docs/database-schema.md"
      provides: "Documentatie van Q&A tabellen, RLS policies, en indexen"
      contains: "## `questions` tabel"
    - path: "docs/CHANGELOG.md"
      provides: "Changelog entry voor Phase 13"
      contains: "Phase 13"
  key_links:
    - from: "public.answers"
      to: "public.questions"
      via: "question_id FK with ON DELETE CASCADE"
      pattern: "references public\\.questions\\(id\\)"
    - from: "public.answer_replies"
      to: "public.answers"
      via: "answer_id FK with ON DELETE CASCADE"
      pattern: "references public\\.answers\\(id\\)"
    - from: "RLS INSERT policies"
      to: "auth.uid()"
      via: "(select auth.uid()) cache-pattern"
      pattern: "select auth\\.uid\\(\\)"
---

<objective>
Maak het Q&A-datamodel aan in Supabase: drie tabellen (questions, answers, answer_replies) met Row Level Security, updated_at triggers, en performance-indexes.

Purpose: Phase 14 (Q&A API) en Phase 15 (Q&A frontend) vereisen dat dit datamodel bestaat. Alle drie tabellen moeten RLS-beveiligd zijn zodat anonieme bezoekers kunnen lezen maar alleen ingelogde gebruikers kunnen schrijven.

Output: SQL migratiebestand dat in de Supabase Dashboard uitgevoerd kan worden, plus bijgewerkte schema-documentatie.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

# Bestaande migratiepatronen (volg dit exact)
@supabase/migrations/00001_profiles.sql

# Bestaande schemadocumentatie (uitbreiden, niet vervangen)
@docs/database-schema.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SQL migration aanmaken — tabellen, RLS, triggers, indexes</name>
  <files>supabase/migrations/00002_qa_schema.sql</files>
  <action>
Maak het bestand `supabase/migrations/00002_qa_schema.sql` aan met exact de volgende structuur (volg het patroon van 00001_profiles.sql):

**Header en structuur:**
```sql
-- Migration: 00002_qa_schema
-- Phase 13: Q&A Datamodel
-- Description: Creates questions, answers, and answer_replies tables with RLS,
--              updated_at triggers, and performance indexes for the Q&A platform.
--
-- Run this SQL in the Supabase Dashboard SQL Editor.
```

**Section 1: Tabellen aanmaken**

```sql
-- questions table
create table public.questions (
  id uuid not null default gen_random_uuid(),
  user_id uuid not null references auth.users(id) on delete cascade,
  title text not null,
  body text not null,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (id)
);

-- answers table
create table public.answers (
  id uuid not null default gen_random_uuid(),
  question_id uuid not null references public.questions(id) on delete cascade,
  user_id uuid not null references auth.users(id) on delete cascade,
  body text not null,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (id)
);

-- answer_replies table
create table public.answer_replies (
  id uuid not null default gen_random_uuid(),
  answer_id uuid not null references public.answers(id) on delete cascade,
  user_id uuid not null references auth.users(id) on delete cascade,
  body text not null,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (id)
);
```

**Section 2: RLS inschakelen**

```sql
alter table public.questions enable row level security;
alter table public.answers enable row level security;
alter table public.answer_replies enable row level security;
```

**Section 3: Updated_at triggers**

De functie `public.update_updated_at_column()` bestaat al (aangemaakt in 00001_profiles.sql). Gebruik `create or replace` zodat het idempotent is. Maak alleen de triggers aan voor de nieuwe tabellen:

```sql
create or replace function public.update_updated_at_column()
returns trigger
language plpgsql
as $$
begin
  new.updated_at = now();
  return new;
end;
$$;

create trigger update_questions_updated_at
  before update on public.questions
  for each row execute procedure public.update_updated_at_column();

create trigger update_answers_updated_at
  before update on public.answers
  for each row execute procedure public.update_updated_at_column();

create trigger update_answer_replies_updated_at
  before update on public.answer_replies
  for each row execute procedure public.update_updated_at_column();
```

**Section 4: RLS Policies**

Gebruik `(select auth.uid())` (NIET `auth.uid()`) in alle policies — dit is besluit 04-01-D2 dat per-statement caching inschakelt voor 20x performance.

Voor alle drie tabellen dezelfde policy-structuur:

```sql
-- questions policies
create policy "Questions are publicly readable"
  on public.questions
  for select
  using ( true );

create policy "Authenticated users can insert questions"
  on public.questions
  for insert
  to authenticated
  with check ( user_id = (select auth.uid()) );

create policy "Users can update their own questions"
  on public.questions
  for update
  to authenticated
  using ( user_id = (select auth.uid()) )
  with check ( user_id = (select auth.uid()) );

create policy "Users can delete their own questions"
  on public.questions
  for delete
  to authenticated
  using ( user_id = (select auth.uid()) );

-- answers policies (zelfde structuur, vervang questions door answers, user_id check blijft)
create policy "Answers are publicly readable"
  on public.answers
  for select
  using ( true );

create policy "Authenticated users can insert answers"
  on public.answers
  for insert
  to authenticated
  with check ( user_id = (select auth.uid()) );

create policy "Users can update their own answers"
  on public.answers
  for update
  to authenticated
  using ( user_id = (select auth.uid()) )
  with check ( user_id = (select auth.uid()) );

create policy "Users can delete their own answers"
  on public.answers
  for delete
  to authenticated
  using ( user_id = (select auth.uid()) );

-- answer_replies policies (zelfde structuur)
create policy "Answer replies are publicly readable"
  on public.answer_replies
  for select
  using ( true );

create policy "Authenticated users can insert answer replies"
  on public.answer_replies
  for insert
  to authenticated
  with check ( user_id = (select auth.uid()) );

create policy "Users can update their own answer replies"
  on public.answer_replies
  for update
  to authenticated
  using ( user_id = (select auth.uid()) )
  with check ( user_id = (select auth.uid()) );

create policy "Users can delete their own answer replies"
  on public.answer_replies
  for delete
  to authenticated
  using ( user_id = (select auth.uid()) );
```

**Section 5: Indexes**

```sql
-- questions: user_id voor "vragen van gebruiker", created_at voor sortering
create index idx_questions_user_id on public.questions(user_id);
create index idx_questions_created_at on public.questions(created_at desc);

-- answers: question_id voor "antwoorden bij vraag", user_id, created_at
create index idx_answers_question_id on public.answers(question_id);
create index idx_answers_user_id on public.answers(user_id);
create index idx_answers_created_at on public.answers(created_at desc);

-- answer_replies: answer_id voor "reacties bij antwoord", user_id, created_at
create index idx_answer_replies_answer_id on public.answer_replies(answer_id);
create index idx_answer_replies_user_id on public.answer_replies(user_id);
create index idx_answer_replies_created_at on public.answer_replies(created_at desc);
```
  </action>
  <verify>
Controleer het bestand bestaat en bevat alle vereiste secties:
- `grep "create table public.questions" supabase/migrations/00002_qa_schema.sql`
- `grep "create table public.answers" supabase/migrations/00002_qa_schema.sql`
- `grep "create table public.answer_replies" supabase/migrations/00002_qa_schema.sql`
- `grep "enable row level security" supabase/migrations/00002_qa_schema.sql` (verwacht 3 resultaten)
- `grep "select auth.uid()" supabase/migrations/00002_qa_schema.sql` (verwacht 12 resultaten — 4 policies per tabel x 3 tabellen, maar alleen de non-select policies)
- `grep "create index" supabase/migrations/00002_qa_schema.sql` (verwacht 9 indexen)
  </verify>
  <done>
Het bestand `supabase/migrations/00002_qa_schema.sql` bestaat en bevat:
- Alle 3 tabellen met correcte FKs en ON DELETE CASCADE
- RLS ingeschakeld op alle 3 tabellen
- Updated_at triggers voor alle 3 tabellen
- 4 RLS policies per tabel (SELECT/INSERT/UPDATE/DELETE) = 12 policies totaal
- 9 performance indexes (3 per tabel)
- Consistent gebruik van `(select auth.uid())` in alle policy conditions
  </done>
</task>

<task type="auto">
  <name>Task 2: docs/database-schema.md uitbreiden met Q&A sectie + CHANGELOG bijwerken</name>
  <files>docs/database-schema.md, docs/CHANGELOG.md</files>
  <action>
**docs/database-schema.md:**

Voeg een Q&A sectie toe aan het bestaande bestand. Verwijder de placeholder in de "Toekomstige uitbreidingen" sectie die `questions`, `answers` vermeldt. Voeg VOOR de "Toekomstige uitbreidingen" sectie een nieuw blok toe:

```markdown
---

## Q&A Tabellen

Aangemaakt in Phase 13. Ondersteunt de Q&A-functionaliteit van het platform.

### `questions` tabel

| Kolom | Type | Nullable | Default | Beschrijving |
|-------|------|----------|---------|--------------|
| `id` | `uuid` | NOT NULL | `gen_random_uuid()` | Primary key |
| `user_id` | `uuid` | NOT NULL | - | Foreign key naar `auth.users(id)`, cascade delete |
| `title` | `text` | NOT NULL | - | Titel van de vraag |
| `body` | `text` | NOT NULL | - | Inhoud van de vraag (Markdown) |
| `created_at` | `timestamptz` | NOT NULL | `now()` | Aanmaakdatum |
| `updated_at` | `timestamptz` | NOT NULL | `now()` | Datum laatste wijziging |

### `answers` tabel

| Kolom | Type | Nullable | Default | Beschrijving |
|-------|------|----------|---------|--------------|
| `id` | `uuid` | NOT NULL | `gen_random_uuid()` | Primary key |
| `question_id` | `uuid` | NOT NULL | - | Foreign key naar `public.questions(id)`, cascade delete |
| `user_id` | `uuid` | NOT NULL | - | Foreign key naar `auth.users(id)`, cascade delete |
| `body` | `text` | NOT NULL | - | Inhoud van het antwoord (Markdown) |
| `created_at` | `timestamptz` | NOT NULL | `now()` | Aanmaakdatum |
| `updated_at` | `timestamptz` | NOT NULL | `now()` | Datum laatste wijziging |

### `answer_replies` tabel

| Kolom | Type | Nullable | Default | Beschrijving |
|-------|------|----------|---------|--------------|
| `id` | `uuid` | NOT NULL | `gen_random_uuid()` | Primary key |
| `answer_id` | `uuid` | NOT NULL | - | Foreign key naar `public.answers(id)`, cascade delete |
| `user_id` | `uuid` | NOT NULL | - | Foreign key naar `auth.users(id)`, cascade delete |
| `body` | `text` | NOT NULL | - | Inhoud van de reactie (Markdown) |
| `created_at` | `timestamptz` | NOT NULL | `now()` | Aanmaakdatum |
| `updated_at` | `timestamptz` | NOT NULL | `now()` | Datum laatste wijziging |

**Cascade deletes:**
- Als een `question` verwijderd wordt, worden alle bijbehorende `answers` automatisch verwijderd.
- Als een `answer` verwijderd wordt, worden alle bijbehorende `answer_replies` automatisch verwijderd.
- Als een gebruiker uit `auth.users` verwijderd wordt, worden alle Q&A-bijdragen van die gebruiker verwijderd.

---

## Q&A RLS Policies

Alle drie Q&A-tabellen volgen hetzelfde RLS-patroon:

| Policy | Operatie | Rol | Voorwaarde | Beschrijving |
|--------|----------|-----|------------|--------------|
| **[Tabel] are publicly readable** | SELECT | `public` | `true` | Anonieme bezoekers kunnen alle Q&A-content lezen |
| **Authenticated users can insert [tabel]** | INSERT | `authenticated` | `user_id = (select auth.uid())` | Ingelogde gebruikers kunnen posten |
| **Users can update their own [tabel]** | UPDATE | `authenticated` | `user_id = (select auth.uid())` | Gebruikers kunnen alleen eigen rijen wijzigen |
| **Users can delete their own [tabel]** | DELETE | `authenticated` | `user_id = (select auth.uid())` | Gebruikers kunnen alleen eigen rijen verwijderen |

**Verschil met profiles:** Q&A-tabellen hebben wél INSERT en DELETE policies (gebruikers plaatsen actief content), terwijl profiles alleen via triggers worden aangemaakt.

---

## Q&A Indexes

| Index | Tabel | Kolom(men) | Doel |
|-------|-------|------------|------|
| `idx_questions_user_id` | `questions` | `user_id` | Vragen ophalen per gebruiker |
| `idx_questions_created_at` | `questions` | `created_at DESC` | Sorteren op nieuwste vragen |
| `idx_answers_question_id` | `answers` | `question_id` | Antwoorden ophalen bij een vraag |
| `idx_answers_user_id` | `answers` | `user_id` | Antwoorden per gebruiker |
| `idx_answers_created_at` | `answers` | `created_at DESC` | Sorteren op nieuwste antwoorden |
| `idx_answer_replies_answer_id` | `answer_replies` | `answer_id` | Reacties ophalen bij een antwoord |
| `idx_answer_replies_user_id` | `answer_replies` | `user_id` | Reacties per gebruiker |
| `idx_answer_replies_created_at` | `answer_replies` | `created_at DESC` | Sorteren op nieuwste reacties |

---
```

Pas ook de metadata onderaan het bestand aan:
- `**Laatst bijgewerkt:**` naar `2026-02-10`
- `**Fase:**` naar `13 - Q&A Datamodel`
- `**Migratie versie:**` naar `00002`

Verwijder in de "Toekomstige uitbreidingen" sectie de bullet over `questions`, `answers`, `question_votes`, `answer_votes` — die zijn nu geimplementeerd.

---

**docs/CHANGELOG.md:**

Voeg bovenaan (of in het meest recente blok) een entry toe voor Phase 13:

```markdown
## Phase 13 — Q&A Datamodel (2026-02-10)

### Toegevoegd
- `supabase/migrations/00002_qa_schema.sql`: Q&A database schema met questions, answers, en answer_replies tabellen
- RLS policies op alle drie tabellen: publiek leesbaar, authenticated users kunnen schrijven, eigen rijen bijwerken/verwijderen
- Updated_at triggers voor alle drie Q&A-tabellen
- 9 performance indexes voor efficiënte queries (question_id, answer_id, user_id, created_at)
- `docs/database-schema.md`: uitgebreid met Q&A tabel documentatie, RLS policies, en indexen overzicht
```
  </action>
  <verify>
- Controleer dat `docs/database-schema.md` de sectie `## Q&A Tabellen` bevat
- Controleer dat `docs/database-schema.md` `## Q&A RLS Policies` bevat
- Controleer dat `docs/database-schema.md` `## Q&A Indexes` bevat
- Controleer dat `docs/CHANGELOG.md` een entry heeft voor Phase 13
- Controleer dat de metadata onderaan database-schema.md `Fase: 13` vermeldt
  </verify>
  <done>
`docs/database-schema.md` bevat volledige Q&A sectie (tabellen, RLS, indexes) en bijgewerkte metadata. `docs/CHANGELOG.md` heeft een Phase 13 entry met de gemaakte wijzigingen.
  </done>
</task>

</tasks>

<verification>
Na uitvoering van beide taken:

1. Bestand `supabase/migrations/00002_qa_schema.sql` bestaat en bevat alle verwachte SQL statements
2. `docs/database-schema.md` heeft drie nieuwe secties: Q&A Tabellen, Q&A RLS Policies, Q&A Indexes
3. `docs/CHANGELOG.md` heeft een Phase 13 entry
4. Geen frontend code aangemaakt in deze fase — dit is puur een database/docs fase

De migration is klaar om uitgevoerd te worden in de Supabase Dashboard SQL Editor (zelfde proces als 00001_profiles.sql).
</verification>

<success_criteria>
- `supabase/migrations/00002_qa_schema.sql` bestaat met 3 tabellen, 3x RLS enabled, 12 RLS policies, 3 triggers, 9 indexes
- Alle RLS INSERT/UPDATE/DELETE policies gebruiken `(select auth.uid())` (niet `auth.uid()`)
- Cascade chain werkt: question verwijderen cascade naar answers, answer verwijderen cascade naar answer_replies
- `docs/database-schema.md` gedocumenteerd met Q&A schema (tabellen, RLS, indexes)
- `docs/CHANGELOG.md` bijgewerkt voor Phase 13
- Geen TypeScript, geen React componenten — pure SQL + docs
</success_criteria>

<output>
Na voltooiing, maak `.planning/phases/13-qa-datamodel/13-01-SUMMARY.md` aan met:

```markdown
---
phase: 13-qa-datamodel
plan: 01
status: complete
subsystem: database
tech-stack:
  used: [PostgreSQL, Supabase RLS, SQL]
  added: []
affects: [14-qa-api, 15-qa-frontend]
key-files:
  - supabase/migrations/00002_qa_schema.sql
  - docs/database-schema.md
decisions: []
---

## Wat is gebouwd

SQL migration `00002_qa_schema.sql` aangemaakt met het volledige Q&A datamodel:

- **3 tabellen:** questions, answers, answer_replies met uuid primary keys en timestamptz tijdstempels
- **Foreign key cascade chain:** answer_replies -> answers -> questions -> auth.users
- **RLS:** ingeschakeld op alle 3 tabellen; publiek SELECT, authenticated INSERT/UPDATE/DELETE op eigen rijen
- **Triggers:** updated_at auto-update op alle 3 tabellen via `update_updated_at_column()`
- **9 indexes:** question_id, answer_id, user_id en created_at DESC per relevante tabel
- `docs/database-schema.md` uitgebreid met Q&A sectie
- `docs/CHANGELOG.md` bijgewerkt voor Phase 13

## Hoe de migration uit te voeren

1. Open Supabase Dashboard -> SQL Editor -> New Query
2. Plak inhoud van `supabase/migrations/00002_qa_schema.sql`
3. Klik Run — verwacht: geen errors, alle statements groen

## Klaar voor Phase 14

Phase 14 (Q&A API) kan nu:
- `SELECT` uitvoeren op questions/answers/answer_replies zonder auth
- `INSERT` met authenticated users via Supabase client
- Queries met `question_id` filter voor "antwoorden bij vraag" (index aanwezig)
- Queries met `answer_id` filter voor "reacties bij antwoord" (index aanwezig)
```
</output>
