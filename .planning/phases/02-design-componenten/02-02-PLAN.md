---
phase: 02-design-componenten
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - components/ui/button.tsx
  - app/page.tsx
  - docs/CHANGELOG.md
autonomous: true

must_haves:
  truths:
    - "At least one reusable UI component (Button) exists and is used on the homepage"
    - "Button supports multiple variants (default, secondary, outline, ghost, link, destructive) and sizes (default, sm, lg, icon)"
    - "Homepage uses the Button component to demonstrate the design system pattern"
    - "Changelog documents Phase 2 additions"
  artifacts:
    - path: "components/ui/button.tsx"
      provides: "Reusable Button component with variant and size props"
      exports: ["Button", "buttonVariants"]
      min_lines: 40
    - path: "app/page.tsx"
      provides: "Updated homepage using Button component and semantic token classes"
      contains: "Button"
    - path: "docs/CHANGELOG.md"
      provides: "Phase 2 changelog entry"
      contains: "Fase 2"
  key_links:
    - from: "components/ui/button.tsx"
      to: "lib/utils.ts"
      via: "import { cn }"
      pattern: "import.*cn.*from.*@/lib/utils"
    - from: "app/page.tsx"
      to: "components/ui/button.tsx"
      via: "import { Button }"
      pattern: "import.*Button.*from.*@/components/ui/button"
    - from: "components/ui/button.tsx"
      to: "class-variance-authority"
      via: "import { cva }"
      pattern: "import.*cva.*from.*class-variance-authority"
---

<objective>
Add the shadcn/ui Button component as the first reusable UI primitive, update the homepage to use it (demonstrating the design system pattern), and update project documentation.

Purpose: Establish the component library pattern (shadcn/ui + cva + cn()) so future phases can add components consistently. The Button on the homepage proves the pattern works end-to-end.
Output: Button component in components/ui/, updated homepage, Phase 2 changelog entry.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-componenten/02-RESEARCH.md
@.planning/phases/02-design-componenten/02-01-SUMMARY.md
@app/page.tsx
@lib/utils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Button component and update homepage</name>
  <files>
    components/ui/button.tsx
    app/page.tsx
  </files>
  <action>
**Step 1: Create `components/ui/button.tsx`** following the exact shadcn/ui pattern for Tailwind v3:

```typescript
import * as React from 'react'
import { Slot } from '@radix-ui/react-slot'
import { cva, type VariantProps } from 'class-variance-authority'

import { cn } from '@/lib/utils'

const buttonVariants = cva(
  'inline-flex items-center justify-center whitespace-nowrap rounded-md text-sm font-medium ring-offset-background transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:pointer-events-none disabled:opacity-50',
  {
    variants: {
      variant: {
        default: 'bg-primary text-primary-foreground hover:bg-primary/90',
        destructive: 'bg-destructive text-destructive-foreground hover:bg-destructive/90',
        outline: 'border border-input bg-background hover:bg-accent hover:text-accent-foreground',
        secondary: 'bg-secondary text-secondary-foreground hover:bg-secondary/80',
        ghost: 'hover:bg-accent hover:text-accent-foreground',
        link: 'text-primary underline-offset-4 hover:underline',
      },
      size: {
        default: 'h-10 px-4 py-2',
        sm: 'h-9 rounded-md px-3',
        lg: 'h-11 rounded-md px-8',
        icon: 'h-10 w-10',
      },
    },
    defaultVariants: {
      variant: 'default',
      size: 'default',
    },
  }
)

export interface ButtonProps
  extends React.ButtonHTMLAttributes<HTMLButtonElement>,
    VariantProps<typeof buttonVariants> {
  asChild?: boolean
}

const Button = React.forwardRef<HTMLButtonElement, ButtonProps>(
  ({ className, variant, size, asChild = false, ...props }, ref) => {
    const Comp = asChild ? Slot : 'button'
    return (
      <Comp
        className={cn(buttonVariants({ variant, size, className }))}
        ref={ref}
        {...props}
      />
    )
  }
)
Button.displayName = 'Button'

export { Button, buttonVariants }
```

Key details:
- Uses `@radix-ui/react-slot` for the `asChild` pattern (enables using Button as a Link wrapper)
- Uses `cva` from class-variance-authority for type-safe variant definitions
- Uses `cn()` from `@/lib/utils` for class merging
- All color classes are semantic tokens (`bg-primary`, `text-primary-foreground`) -- no raw colors
- Exported as named exports: `Button` (component) and `buttonVariants` (for use outside the component)

**Step 2: Update `app/page.tsx`** to use the Button component and switch from raw Tailwind color classes to semantic token classes:

Replace the current homepage content. The page should:
- Remove `<main>` wrapper (the root layout now provides `<main className="flex-1">`)
- Use a section/div instead, centered in the container
- Keep the heading "Nederlandse AI Assistenten Hub" and Dutch subtitle text
- Replace raw color classes (`text-gray-700`, `dark:text-gray-300`, etc.) with semantic tokens (`text-muted-foreground`)
- Add 1-2 Button components to demonstrate usage. For example:
  - A primary Button: "Ontdek OpenClaw" (links to /openclaw concept)
  - A secondary/outline Button: "Bekijk tutorials" (links to /openclaw/tutorials concept)
- Use the `asChild` pattern to wrap Buttons around `next/link` for navigation:

```tsx
import Link from 'next/link'
import { Button } from '@/components/ui/button'

<Button asChild>
  <Link href="/openclaw">Ontdek OpenClaw</Link>
</Button>
<Button variant="outline" asChild>
  <Link href="/openclaw/tutorials">Bekijk tutorials</Link>
</Button>
```

- Update the phase indicator to "Fase 2: Design & componenten voltooid"
- Keep the page as a Server Component (no 'use client' needed -- Buttons with asChild and Link are not interactive client components)

Important: The page no longer needs `min-h-screen` or `flex` centering since the layout handles that. Use `container` class for consistent width, and center content within using flexbox or text-center.
  </action>
  <verify>
Run `npm run build` -- must compile without errors. Verify:
- `components/ui/button.tsx` exports `Button` and `buttonVariants`
- `app/page.tsx` imports and renders at least one `Button` component
- `app/page.tsx` does NOT use raw color classes (`text-gray-700`, `dark:text-gray-300`, etc.)
- `app/page.tsx` uses semantic token classes (`text-muted-foreground`, `bg-background`, etc.)
- No 'use client' directive in page.tsx
  </verify>
  <done>
Button component exists at components/ui/button.tsx with 6 variants and 4 sizes. Homepage uses Button with asChild pattern for navigation links, demonstrating the design system end-to-end. All raw color classes replaced with semantic tokens.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update changelog and roadmap</name>
  <files>
    docs/CHANGELOG.md
    roadmap/ROADMAP.md
  </files>
  <action>
**Step 1: Update `docs/CHANGELOG.md`** -- Add a Phase 2 entry at the top of the changelog (below any header). Include:

```markdown
## Fase 2 - Design & componenten

### Toegevoegd
- Design tokens: HSL-based CSS custom properties in `globals.css` (light + dark mode)
- Tailwind config: Extended with semantic color tokens, container config (centered, 1400px max), border-radius tokens
- Inter font: Loaded via `next/font/google`, applied globally as `font-sans`
- `cn()` utility: Class merging helper at `lib/utils.ts` (clsx + tailwind-merge)
- Header component: Sticky header with site name and navigation (Home, OpenClaw, Blog, Q&A)
- Footer component: Copyright with dynamic year, placeholder links (Privacy, Gebruiksvoorwaarden)
- Button component: Reusable UI primitive with 6 variants and 4 sizes (shadcn/ui pattern)
- Layout shell: Flex-column structure in root layout with Header, main, Footer

### Gewijzigd
- `globals.css`: Replaced RGB variables with HSL design token system
- `tailwind.config.ts`: Extended with semantic colors, container, font, border-radius, animations plugin
- `app/layout.tsx`: Added Inter font, Header, Footer, flex-column layout
- `app/page.tsx`: Updated to use Button component and semantic token classes

### Dependencies
- Added: class-variance-authority, clsx, tailwind-merge, tailwindcss-animate, lucide-react, @radix-ui/react-slot
```

**Step 2: Update `roadmap/ROADMAP.md`** -- Mark Phase 2 as having plans:
- Find the "Fase 2 - Design & componenten" section
- Update the checkbox line to include plan references
- Keep the phase unchecked (it's not complete until execution finishes)
  </action>
  <verify>
- `docs/CHANGELOG.md` contains "Fase 2" entry with the additions listed above
- `roadmap/ROADMAP.md` references Phase 2 plans
  </verify>
  <done>
Changelog documents all Phase 2 additions (design tokens, Header, Footer, Button, font, cn() utility, dependencies). Roadmap updated with Phase 2 plan references.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes without errors
2. `components/ui/button.tsx` exports Button with 6 variants and 4 sizes
3. `app/page.tsx` uses Button component with asChild + Link pattern
4. `app/page.tsx` uses only semantic token classes (no raw colors)
5. `docs/CHANGELOG.md` has Phase 2 entry
6. `roadmap/ROADMAP.md` references Phase 2 plans
</verification>

<success_criteria>
- Button component: Accessible, variant-based, uses cn() and cva
- Homepage: Demonstrates Button with navigation links, uses semantic tokens throughout
- Documentation: Changelog and roadmap updated
- Build: Clean `npm run build` with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-design-componenten/02-02-SUMMARY.md`
</output>
