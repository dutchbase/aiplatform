---
phase: 19-eerste-content
plan: 01
type: execute
wave: 1
depends_on: [18-01]
files_modified:
  - app/openclaw/installatie/page.tsx
  - lib/data/tutorials.ts
  - docs/CHANGELOG.md
autonomous: true

must_haves:
  truths:
    - "The installation page /openclaw/installatie contains no placeholder notice boxes or 'volgt in Phase 19' text"
    - "The installation page shows a visible publication date (e.g. 'Gepubliceerd op: 9 februari 2026')"
    - "All three tutorials have substantive Dutch content with at least 4 steps each and code examples where applicable"
    - "No tutorial or installation page contains the phrase 'komt binnenkort', 'volgt later', or any placeholder notice box"
    - "Tutorial detail pages display 'Laatst bijgewerkt' date (already rendered via tutorial.lastUpdated field)"
    - "Tutorial slugs remain eerste-stappen, configuratie, tips (unchanged — generateStaticParams depends on these)"
  artifacts:
    - path: "app/openclaw/installatie/page.tsx"
      provides: "Full installation guide with requirements, numbered steps with code blocks, verification steps, publication date"
      contains: "Gepubliceerd"
    - path: "lib/data/tutorials.ts"
      provides: "Three complete Tutorial objects with intro + 4 steps each, code examples in content fields"
      exports: ["tutorials", "Tutorial"]
    - path: "docs/CHANGELOG.md"
      provides: "Phase 19 changelog entry"
      contains: "Phase 19"
  key_links:
    - from: "app/openclaw/tutorials/[slug]/page.tsx"
      to: "lib/data/tutorials.ts"
      via: "import { tutorials }"
      pattern: "tutorials\\.find"
    - from: "app/openclaw/tutorials/[slug]/page.tsx"
      to: "tutorial.lastUpdated"
      via: "formatDate(tutorial.lastUpdated)"
      pattern: "lastUpdated"
---

<objective>
Fill Phase 19's first-content requirement: replace all placeholder content in the OpenClaw installation page and the three tutorial data objects with complete, professional Dutch content. Add a visible publication date to the installation page. Update CHANGELOG.

Purpose: The platform has had structural pages with placeholder notices since Phase 9-10. Phase 19 is the content phase — these pages must read as finished editorial content before the platform goes toward production.

Output:
- app/openclaw/installatie/page.tsx — complete installation guide, no placeholder boxes, publication date visible
- lib/data/tutorials.ts — three Tutorial objects with substantive intro + 4 steps each (including code examples)
- docs/CHANGELOG.md — Phase 19 entry
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@app/openclaw/installatie/page.tsx
@lib/data/tutorials.ts
@app/openclaw/tutorials/[slug]/page.tsx
@docs/CHANGELOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Installation page — replace placeholders with full Dutch content and add publication date</name>
  <files>app/openclaw/installatie/page.tsx</files>
  <action>
Read app/openclaw/installatie/page.tsx first (it already exists). Then rewrite it with the following changes:

1. REMOVE all three placeholder notice boxes (the p elements with class "bg-accent/30" containing "volgt in Phase 19" / "wordt verder uitgebreid" / "volgen later").

2. EXPAND each section with full content:

**Section "Vereisten"** — replace placeholder with:
- Heading stays: "Vereisten"
- Two sub-categories: "Besturingssysteem" (Windows 10/11, macOS 12+, Ubuntu 20.04+) and "Software"
- Software list: Node.js 18 of hoger (https://nodejs.org), npm 9+ of yarn 1.22+ (meegeleverd met Node.js), Git 2.30 of hoger (https://git-scm.com)
- A note: "Controleer je versies via de terminal: node --version, npm --version, git --version"
- Show a code block with those three commands

**Section "Installatie"** — replace placeholder with 5 numbered steps, each with explanatory text AND a code block:
- Stap 1: Repository klonen
  ```bash
  git clone https://github.com/openclaw/openclaw.git
  cd openclaw
  ```
- Stap 2: Dependencies installeren
  ```bash
  npm install
  ```
- Stap 3: Omgevingsvariabelen instellen — copy .env.example to .env and explain the API key field
  ```bash
  cp .env.example .env
  ```
  Then open .env and fill in OPENCLAW_API_KEY with your key from the OpenClaw dashboard.
- Stap 4: OpenClaw bouwen
  ```bash
  npm run build
  ```
- Stap 5: OpenClaw globaal beschikbaar maken (optional but recommended)
  ```bash
  npm install -g .
  ```

**Section "Controleren of het werkt"** — replace placeholder with:
- Intro: "Voer het volgende commando uit om te bevestigen dat OpenClaw correct is geïnstalleerd:"
- Code block:
  ```bash
  openclaw --version
  ```
- Expected output note: "Je zou een versienummer moeten zien, zoals: OpenClaw v1.2.0"
- Second check: run `openclaw --help` to see all available commands
- Code block for that too

3. ADD a publication date below the page title/intro, before the Vereisten section. Pattern (match the tutorial detail page style):
```tsx
<p className="text-sm text-muted-foreground mt-2">
  Gepubliceerd op: 9 februari 2026
</p>
```

4. For code blocks, use a `<pre>` + `<code>` pattern with Tailwind classes for styling. No external syntax highlighter — keep it simple:
```tsx
<pre className="bg-muted rounded-md p-4 overflow-x-auto text-sm">
  <code>{`your code here`}</code>
</pre>
```

Keep all existing JSX structure: the container/max-w-3xl wrapper, OpenClawNav, the CTA section at the bottom, and the metadata export. Do not change the metadata object.

The page must be a Server Component (no 'use client'). All imports already present should be kept.
  </action>
  <verify>
    Run: pnpm type-check (or npx tsc --noEmit)
    Visually confirm: no placeholder notice boxes visible in the file
    Grep check: grep -n "volgt in Phase\|wordt verder uitgebreid\|volgen later\|bg-accent/30" app/openclaw/installatie/page.tsx — should return no matches
  </verify>
  <done>
    - Installation page has no placeholder notice boxes
    - All three sections (Vereisten, Installatie, Controleren) have full Dutch content with code blocks
    - A "Gepubliceerd op" date is visible below the intro paragraph
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Tutorial data — expand all three tutorials with richer content and code examples</name>
  <files>lib/data/tutorials.ts</files>
  <action>
Read lib/data/tutorials.ts first to see the current Tutorial interface and existing content. The interface is:

```typescript
export interface Tutorial {
  slug: string
  title: string
  description: string
  lastUpdated: string
  content: {
    intro: string
    steps: Array<{
      heading: string
      content: string
    }>
  }
}
```

The tutorial detail page renders step.content as plain text inside a `<p>` tag (no markdown rendering). Therefore: content fields are plain strings. Code examples must be described in prose or shown inline with backtick notation (or described as "voer het volgende commando uit: `openclaw start`"). Do NOT use markdown syntax expecting a renderer.

The three existing tutorials (slugs: eerste-stappen, configuratie, tips) already have 3 steps each with decent intro text. Phase 19 expands them to 4 steps each, with richer and more concrete content.

**Tutorial 1 — slug: 'eerste-stappen'**
Keep existing intro. Replace/expand steps to 4:

Step 1 — "OpenClaw opstarten":
Content: "Open een terminal in de map van je project en typ: openclaw start. OpenClaw analyseert automatisch de bestanden in je werkmap en laadt de projectcontext. Je ziet een bevestiging zodra de assistent gereed is, inclusief het aantal geïndexeerde bestanden. Zorg dat je werkmap de root van je project is — OpenClaw gebruikt package.json of een .openclaw-bestand als ankerpunt."

Step 2 — "Je eerste vraag stellen":
Content: "Typ je vraag direct in de prompt die OpenClaw toont. Wees concreet: 'Wat doet de functie getUserById in src/lib/users.ts?' werkt beter dan 'Leg mijn code uit.' OpenClaw doorzoekt de geïndexeerde bestanden, vindt de relevante context en geeft een antwoord met verwijzing naar de betreffende regels. Je kunt ook een bestandspad meegeven: 'Analyseer @src/lib/users.ts'."

Step 3 — "Code genereren":
Content: "Beschrijf de gewenste functionaliteit zo specifiek mogelijk. Gebruik aanwijzingen als 'in TypeScript', 'gebruik de fetch API', of 'schrijf een async functie'. Voorbeeld: 'Schrijf een async TypeScript-functie die een gebruiker op ID ophaalt uit Supabase en undefined teruggeeft als de gebruiker niet bestaat.' OpenClaw genereert de code en legt uit welke keuzes het heeft gemaakt."

Step 4 — "Je sessie afsluiten en hervatten":
Content: "Type exit of druk op Ctrl+C om een sessie te beëindigen. OpenClaw slaat de gespreksgeschiedenis automatisch op in .openclaw/history. Bij de volgende start laad je een eerdere sessie via: openclaw start --resume. Dit is handig bij langere projecten waarbij je de context niet opnieuw wilt opbouwen."

**Tutorial 2 — slug: 'configuratie'**
Keep existing intro. Replace/expand steps to 4:

Step 1 — "Het configuratiebestand aanmaken":
Content: "OpenClaw zoekt bij het opstarten naar een .openclaw.json bestand in de root van je project. Als dit bestand niet bestaat, werkt OpenClaw met standaardinstellingen. Maak het bestand aan via: openclaw init. Dit genereert een .openclaw.json met de meestgebruikte opties en hun standaardwaarden. Voeg .openclaw.json toe aan je versiebeheersysteem zodat je teamconfiguratie gedeeld wordt."

Step 2 — "API-sleutel instellen":
Content: "OpenClaw heeft een API-sleutel nodig voor de onderliggende AI-dienst. Sla je sleutel op in een .env bestand: OPENCLAW_API_KEY=jouw-sleutel-hier. Verwijs vanuit .openclaw.json naar de omgevingsvariabele met: \"apiKey\": \"${OPENCLAW_API_KEY}\". Commit nooit je .env bestand naar Git — voeg het toe aan .gitignore. Je API-sleutel vind je in het dashboard van je OpenClaw-account."

Step 3 — "Taal en responsegedrag instellen":
Content: "Stel in .openclaw.json de voorkeurstaal in voor antwoorden: \"language\": \"nl\" voor Nederlands. Configureer ook het detailniveau met \"verbosity\": \"concise\" (kort), \"normal\" (standaard) of \"detailed\" (uitgebreid). Voor codegerelateerde vragen kun je het framework specificeren: \"defaultStack\": [\"typescript\", \"nextjs\", \"supabase\"]. OpenClaw past zijn antwoorden hierop aan zonder dat je dit telkens hoeft te herhalen."

Step 4 — "Bestanden en mappen uitsluiten":
Content: "Gebruik de ignore-optie om mappen uit te sluiten van indexering: \"ignore\": [\"node_modules\", \".next\", \"dist\", \"coverage\"]. Dit versnelt het opstarten en voorkomt dat OpenClaw irrelevante code als context gebruikt. Je kunt ook bestandspatronen opgeven zoals \"**/*.test.ts\" als je geen testbestanden als context wilt laden. Een kleinere, relevante context leidt doorgaans tot betere en snellere antwoorden."

**Tutorial 3 — slug: 'tips'**
Keep existing intro. Replace/expand steps to 4:

Step 1 — "Effectieve prompts schrijven":
Content: "De meest bruikbare prompts zijn concreet, context-rijk en actiegericht. Gebruik het patroon: [actie] + [onderwerp] + [beperking of gewenst formaat]. Slecht: 'fix dit'. Goed: 'Refactor de functie parseUserInput in src/utils/parser.ts zodat deze een Result-type teruggeeft in plaats van een exception te gooien. Gebruik hetzelfde patroon als in src/utils/validator.ts.' Door een referentiebestand te noemen, stuurt OpenClaw de stijl van het antwoord."

Step 2 — "Context slim inzetten":
Content: "OpenClaw indexeert je gehele project, maar jij bepaalt welke context actief is. Prefix een bestandspad met @ om het expliciet als context te markeren: '@src/lib/auth.ts Controleer of deze functie gevoelig is voor session hijacking.' Je kunt meerdere bestanden tegelijk markeren. Voor grote codebases helpt het om context te beperken tot de relevante module — dit geeft gerichte antwoorden en bespaart tokens."

Step 3 — "Veelgemaakte fouten vermijden":
Content: "De meest voorkomende valkuil is een te vage prompt waardoor OpenClaw een generiek antwoord geeft dat niet aansluit bij je codebase. Tweede fout: OpenClaw vragen om iets te 'repareren' zonder de foutmelding mee te sturen. Voeg altijd de volledige foutmelding toe, inclusief stack trace. Derde fout: ervan uitgaan dat OpenClaw wijzigingen automatisch opslaat — het genereert altijd een voorstel dat jij beoordeelt en toepast."

Step 4 — "Workflow-integratie":
Content: "Maak OpenClaw onderdeel van vaste stappen in je ontwikkelworkflow. Gebruik het bij code reviews: 'Beoordeel deze diff op mogelijke bugs en onveilige patronen.' Gebruik het bij het schrijven van tests: 'Schrijf Jest-tests voor alle edge cases van de functie calculateDiscount.' Gebruik het bij documentatie: 'Genereer een JSDoc-commentaarblok voor elke publieke functie in dit bestand.' Door OpenClaw structureel in te zetten verhoog je de consistentie en kwaliteit van je code."

Do not change slugs, titles, descriptions, or the lastUpdated date. Only update content.intro and content.steps for all three tutorials. Keep the Tutorial interface unchanged.
  </action>
  <verify>
    Run: pnpm type-check (or npx tsc --noEmit)
    Count steps per tutorial: each must have exactly 4 steps
    Grep check: grep -c "heading:" lib/data/tutorials.ts — should return 12 (4 steps x 3 tutorials)
  </verify>
  <done>
    - All three tutorials have 4 steps each (total 12 step objects)
    - Each step.content is a non-empty string with substantive Dutch prose
    - No placeholder text remains
    - TypeScript compiles without errors
    - Tutorial slugs unchanged (eerste-stappen, configuratie, tips)
  </done>
</task>

<task type="auto">
  <name>Task 3: CHANGELOG update for Phase 19</name>
  <files>docs/CHANGELOG.md</files>
  <action>
Read docs/CHANGELOG.md first. Insert a new Phase 19 entry at the TOP of the changelog (above the existing Phase 15 entry). Use the same format as existing entries.

Insert:

```markdown
## [Phase 19] - 2026-02-10 - Eerste Content

### Toegevoegd
- **Installatiepagina volledige inhoud** (`app/openclaw/installatie/page.tsx`): alle placeholder-notices vervangen door complete Nederlandstalige handleiding met vereisten (OS, software, versiecontrole), vijf genummerde installatiestappen met codeblokken, en verificatiesectie met verwachte uitvoer
- **Publicatiedatum installatiepagina**: "Gepubliceerd op: 9 februari 2026" zichtbaar onder de intro
- **Tutorial inhoud uitgebreid** (`lib/data/tutorials.ts`): alle drie tutorials (eerste-stappen, configuratie, tips) uitgebreid van 3 naar 4 stappen elk; concrete commando's, configuratievoorbeelden en best-practice-patronen toegevoegd

### Gewijzigd
- `lib/data/tutorials.ts`: content.steps uitgebreid voor alle drie tutorials; Tutorial-interface en slugs ongewijzigd
- `app/openclaw/installatie/page.tsx`: placeholder-notices verwijderd; inhoud van drie secties volledig uitgewerkt; codeblokken toegevoegd met pre/code-elementen

### Technical Details
- Geen nieuwe dependencies — codeblokken via standaard HTML pre/code met Tailwind-classes
- Server Components blijven onaangetast — geen 'use client' toegevoegd
- generateStaticParams onaangetast — slugs ongewijzigd
```
  </action>
  <verify>
    Read docs/CHANGELOG.md and confirm Phase 19 entry appears at the top, above Phase 15.
  </verify>
  <done>
    - CHANGELOG.md contains a Phase 19 entry as the newest entry
    - Entry accurately describes what was changed in Tasks 1 and 2
  </done>
</task>

</tasks>

<verification>
After all three tasks complete:

1. TypeScript check: pnpm type-check or npx tsc --noEmit — must pass with 0 errors
2. Placeholder check: grep -rn "volgt in Phase\|wordt verder uitgebreid\|volgen later\|komt binnenkort" app/openclaw/ — must return 0 matches
3. Publication date present: grep "Gepubliceerd" app/openclaw/installatie/page.tsx — must return 1 match
4. Tutorial step count: grep -c "heading:" lib/data/tutorials.ts — must return 12
5. Slug integrity: grep "slug:" lib/data/tutorials.ts — must show eerste-stappen, configuratie, tips
6. CHANGELOG updated: grep "Phase 19" docs/CHANGELOG.md — must return at least 1 match
</verification>

<success_criteria>
- /openclaw/installatie renders as a complete installation guide with zero placeholder notice boxes
- The installation page shows a Dutch publication date
- All three tutorials at /openclaw/tutorials/[slug] have 4 substantive steps each
- No file contains placeholder language ("volgt later", "binnenkort", "Phase 19" as future reference)
- TypeScript compiles cleanly
- CHANGELOG documents Phase 19 changes
</success_criteria>

<output>
After completion, create `.planning/phases/19-eerste-content/19-01-SUMMARY.md` with:
- Frontmatter: phase, plan, status: complete, files_modified list, decisions made (if any)
- Summary of what was built
- Any deviations from the plan
- Verification results
</output>
