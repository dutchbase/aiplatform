---
phase: 01-nextjs-app-basis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/layout.tsx
  - app/page.tsx
autonomous: true

must_haves:
  truths:
    - "Homepage renders with Dutch welcome text"
    - "Meta tags show correct Dutch title and description"
    - "Open Graph tags present for social sharing"
    - "Page sets lang='nl' for Dutch language"
    - "Dev server runs without errors"
    - "Dark mode respects system preference"
  artifacts:
    - path: "app/layout.tsx"
      provides: "Root layout with comprehensive metadata"
      min_lines: 40
      exports: ["metadata", "default"]
    - path: "app/page.tsx"
      provides: "Homepage Server Component"
      min_lines: 15
      exports: ["default"]
  key_links:
    - from: "app/layout.tsx"
      to: "app/globals.css"
      via: "import statement"
      pattern: "import.*globals\\.css"
    - from: "app/layout.tsx"
      to: "html lang attribute"
      via: "lang='nl' attribute"
      pattern: "<html lang=\"nl\">"
    - from: "app/page.tsx"
      to: "Tailwind classes"
      via: "className attributes"
      pattern: "className=\".*\""
---

<objective>
Enhance the existing Next.js 14 App Router foundation with comprehensive Dutch metadata and improved homepage content, establishing the base for all future phases.

Purpose: Formalize the basic Next.js app structure created in Phase 0 with production-ready metadata, proper Dutch language configuration, and SEO-optimized homepage content that serves as the template for future pages.

Output: A validated Next.js app with comprehensive metadata (SEO, Open Graph, language settings), enhanced Dutch homepage content, and verified dev server operation.
</objective>

<execution_context>
@C:\Users\dutch\OneDrive\Bureaublad\aiassistentplatform\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\dutch\OneDrive\Bureaublad\aiassistentplatform\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\dutch\OneDrive\Bureaublad\aiassistentplatform\CLAUDE.md
@C:\Users\dutch\OneDrive\Bureaublad\aiassistentplatform\prd.md
@C:\Users\dutch\OneDrive\Bureaublad\aiassistentplatform\.planning\phases\01-nextjs-app-basis\01-RESEARCH.md
@C:\Users\dutch\OneDrive\Bureaublad\aiassistentplatform\.planning\codebase\STACK.md
@C:\Users\dutch\OneDrive\Bureaublad\aiassistentplatform\.planning\codebase\CONVENTIONS.md
@C:\Users\dutch\OneDrive\Bureaublad\aiassistentplatform\roadmap\phase-01-nextjs-app-basis.md
</context>

<tasks>

<task type="auto">
  <name>Enhance root layout with comprehensive metadata</name>
  <files>app/layout.tsx</files>
  <action>
Enhance the existing root layout with comprehensive metadata following the patterns from 01-RESEARCH.md:

1. **Expand metadata object** with all SEO-critical fields:
   - Use `title.template` pattern: `'%s | Nederlandse AI Assistenten Hub'` for child page consistency
   - Add keywords array: `['AI assistenten', 'OpenClaw', 'Nederlands', 'tutorials', 'Q&A']`
   - Add authors, creator, publisher fields
   - Set metadataBase to `process.env.NEXT_PUBLIC_BASE_URL || 'http://localhost:3000'` (will be set in future deployment phase)
   - Add alternates.canonical and alternates.languages with `'nl-NL': '/'`
   - Add complete openGraph object with type, locale ('nl_NL'), url, siteName, title, description
   - Add twitter card object with card type and description
   - Add robots object with index:true, follow:true, and googleBot configuration

2. **Add antialiased class to body**: `<body className="antialiased">{children}</body>` for better font rendering

3. **Keep structure minimal**: Remain a Server Component, no 'use client' directive

**Why this approach:**
- Metadata API is Next.js 14 standard (no manual head tags - Research anti-pattern)
- Template pattern ensures consistent titles across all future pages
- Dutch locale (nl_NL) signals primary language to search engines
- metadataBase enables proper Open Graph image URLs in future phases
- Antialiased improves text rendering on all browsers

Reference the complete metadata example in 01-RESEARCH.md lines 311-371 for the exact structure.
  </action>
  <verify>
Run `npm run dev` and check:
1. No TypeScript errors in terminal
2. Visit http://localhost:3000
3. View page source - verify meta tags present: title, description, og:locale, robots
4. Check browser tab shows "Nederlandse AI Assistenten Hub"
  </verify>
  <done>
- app/layout.tsx exports metadata object with title.template, keywords, openGraph, twitter, robots
- metadata.openGraph.locale is 'nl_NL'
- html element has lang="nl"
- body element has antialiased class
- Dev server runs without errors
  </done>
</task>

<task type="auto">
  <name>Enhance homepage with Dutch content</name>
  <files>app/page.tsx</files>
  <action>
Enhance the existing homepage Server Component with better structured Dutch content:

1. **Update main content structure**:
   - Keep existing Tailwind layout (`flex min-h-screen flex-col items-center justify-center`)
   - Add responsive padding: `p-8 md:p-24` instead of just `p-24`
   - Add max-width to center div: `max-w-3xl` for better readability

2. **Enhance heading and copy**:
   - Keep h1: "Nederlandse AI Assistenten Hub"
   - Add responsive text sizing to h1: `text-4xl md:text-5xl font-bold mb-6`
   - Enhance subtitle paragraph with better copy from PRD:
     "Jouw centrale platform voor Nederlandse AI-assistenten, beginnend met OpenClaw"
   - Add text color classes: `text-gray-700 dark:text-gray-300` for dark mode support
   - Add third paragraph with actionable intro:
     "Ontdek tutorials, stel vragen, en leer hoe je AI-assistenten effectief inzet."
   - Style third paragraph: `text-base text-gray-600 dark:text-gray-400`

3. **Update phase status indicator**:
   - Change "Fase 0: Projectsetup voltooid" to "Fase 1: Next.js-appbasis voltooid"
   - Keep small text styling for status

**Why this approach:**
- Server Component by default (no 'use client' needed - Research pattern)
- Responsive text sizing improves mobile experience
- Dark mode text colors ensure readability in both themes (using CSS variables from globals.css)
- Copy aligns with PRD positioning (Section 2.1) and tone of voice (Section 2.3)
- Max-width prevents text from stretching too wide on large screens

Reference the homepage example in 01-RESEARCH.md lines 373-395 for the structure pattern.
  </action>
  <verify>
Run `npm run dev` and check:
1. Visit http://localhost:3000
2. Verify text displays in Dutch with proper hierarchy
3. Resize browser window - verify responsive text sizing works
4. Toggle system dark mode - verify text colors adapt correctly
5. No console errors in browser devtools
  </verify>
  <done>
- Homepage displays "Nederlandse AI Assistenten Hub" heading
- Subtitle mentions "beginnend met OpenClaw"
- Third paragraph mentions "tutorials" and "vragen"
- Text is responsive (md: breakpoint)
- Dark mode text colors applied
- Phase status shows "Fase 1"
  </done>
</task>

<task type="auto">
  <name>Update documentation and commit</name>
  <files>docs/CHANGELOG.md, roadmap/ROADMAP.md</files>
  <action>
Update project documentation to reflect Phase 1 completion:

1. **Update CHANGELOG.md**:
   - Add new entry under `## [Unreleased]` section (or create if missing):
     ```
     ### Added
     - Next.js 14 App Router basis with comprehensive metadata
     - Dutch homepage with responsive design and dark mode support
     - SEO-optimized root layout with Open Graph and Twitter cards
     - Language configuration (lang="nl", locale="nl_NL")
     ```

2. **Update ROADMAP.md**:
   - Change line 31: `- [ ] **1 – Next.js-appbasis**` to `- [x] **1 – Next.js-appbasis**`

3. **Git commit** with conventional format:
   ```bash
   git add app/layout.tsx app/page.tsx docs/CHANGELOG.md roadmap/ROADMAP.md
   git commit -m "$(cat <<'EOF'
   feat(phase-1): enhance Next.js app basis with metadata and Dutch content

   - Add comprehensive metadata (SEO, Open Graph, Twitter cards)
   - Configure Dutch language (lang="nl", locale="nl_NL")
   - Enhance homepage with responsive design and dark mode
   - Update phase status to Fase 1

   Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>
   EOF
   )"
   ```

**Why this approach:**
- CHANGELOG.md tracks all changes per project workflow (CLAUDE.md)
- ROADMAP.md checkbox marks phase complete
- Conventional commit format follows project standards (CONVENTIONS.md)
- Co-authored-by credits Claude per GSD workflow
  </action>
  <verify>
Run these commands:
1. `git status` - verify files staged correctly
2. `git log -1 --pretty=format:"%s"` - verify commit message
3. `cat docs/CHANGELOG.md` - verify entry added
4. `grep "\[x\].*1.*Next.js" roadmap/ROADMAP.md` - verify checkbox marked
  </verify>
  <done>
- CHANGELOG.md has Phase 1 entry
- ROADMAP.md line 31 shows `[x]` for Phase 1
- Git commit exists with message starting "feat(phase-1):"
- All 4 files committed
  </done>
</task>

</tasks>

<verification>
After completing all tasks, verify the entire phase:

1. **Dev server runs cleanly:**
   ```bash
   npm run dev
   ```
   - No TypeScript errors in terminal
   - Server starts on http://localhost:3000

2. **Homepage renders correctly:**
   - Visit http://localhost:3000
   - Verify Dutch heading and content display
   - Verify responsive behavior (resize window)
   - Verify dark mode support (toggle system preference)

3. **Metadata is correct:**
   - View page source (Ctrl+U)
   - Verify `<html lang="nl">` tag
   - Verify meta description includes "OpenClaw"
   - Verify og:locale is "nl_NL"
   - Verify title shows in browser tab

4. **Git history is clean:**
   ```bash
   git log -1
   ```
   - Commit message follows conventional format
   - All 4 files in commit

5. **TypeScript compiles:**
   ```bash
   npm run build
   ```
   - Build completes without errors
   - No type errors reported
</verification>

<success_criteria>
Phase 1 is complete when:

- [ ] Root layout has comprehensive metadata with title.template, keywords, openGraph, twitter, robots
- [ ] Metadata sets Dutch language (lang="nl", locale="nl_NL")
- [ ] Homepage displays enhanced Dutch content with responsive design
- [ ] Dark mode text colors work correctly
- [ ] Dev server runs without errors
- [ ] Build succeeds without TypeScript errors
- [ ] CHANGELOG.md updated with Phase 1 entry
- [ ] ROADMAP.md marks Phase 1 complete
- [ ] Git commit follows conventional format
- [ ] All must_haves verified (Dutch text, metadata, language config, dev server)
</success_criteria>

<output>
After completion, create `.planning/phases/01-nextjs-app-basis/01-01-SUMMARY.md` following the summary template.
</output>
