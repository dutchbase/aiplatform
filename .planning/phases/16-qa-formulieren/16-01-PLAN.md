---
phase: 16-qa-formulieren
plan: 01
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - app/qa/nieuwe-vraag/page.tsx
  - components/qa/ask-question-form.tsx
  - components/qa/answer-form.tsx
  - components/qa/reply-form.tsx
  - app/qa/vraag/[id]/page.tsx
  - app/qa/page.tsx
  - docs/CHANGELOG.md
autonomous: true

must_haves:
  truths:
    - "Niet-ingelogde gebruiker die /qa/nieuwe-vraag bezoekt ziet een login-CTA (niet het formulier)"
    - "Ingelogde gebruiker ziet een formulier met velden 'Titel' en 'Beschrijving' op /qa/nieuwe-vraag"
    - "Na succesvolle vraagstelling wordt de gebruiker doorgestuurd naar /qa/vraag/[id] van de nieuwe vraag"
    - "Client-side validatiefouten (te korte titel/body) worden getoond zonder page reload"
    - "Niet-ingelogde gebruiker op de detailpagina ziet login-CTA in plaats van het antwoordformulier"
    - "Ingelogde gebruiker kan een antwoord plaatsen; na submit toont het formulier een inline succesmelding 'Antwoord geplaatst!' en de textarea wordt geleegd (formulier blijft zichtbaar)"
    - "Per antwoord is er een 'Reageer' knop; klikken toont een inline formulier; na submit wordt de reactie getoond"
    - "Server-side foutmeldingen (bijv. 'Antwoord moet minimaal 10 tekens zijn') worden getoond in het formulier"
  artifacts:
    - path: "app/qa/nieuwe-vraag/page.tsx"
      provides: "Server Component page — auth check, login CTA or form wrapper"
      contains: "createClient().auth.getUser()"
    - path: "components/qa/ask-question-form.tsx"
      provides: "Client Component — vraag stellen form met useFormState"
      exports: ["AskQuestionForm"]
    - path: "components/qa/answer-form.tsx"
      provides: "Client Component — antwoord geven form met useFormState"
      exports: ["AnswerForm"]
    - path: "components/qa/reply-form.tsx"
      provides: "Client Component — reageer toggle + form met useFormState"
      exports: ["ReplyForm"]
    - path: "app/qa/vraag/[id]/page.tsx"
      provides: "Updated detail page — auth check, AnswerForm + ReplyForm integration"
      contains: "AnswerForm"
  key_links:
    - from: "app/qa/nieuwe-vraag/page.tsx"
      to: "components/qa/ask-question-form.tsx"
      via: "import and render AskQuestionForm (only when user is authenticated)"
      pattern: "AskQuestionForm"
    - from: "components/qa/ask-question-form.tsx"
      to: "app/qa/actions.ts createQuestion"
      via: "useFormState(createQuestion, null) and form action"
      pattern: "useFormState.*createQuestion"
    - from: "components/qa/ask-question-form.tsx"
      to: "/qa/vraag/[id]"
      via: "useRouter().push(state.id) when state.id is present"
      pattern: "router\\.push.*state\\.id"
    - from: "app/qa/vraag/[id]/page.tsx"
      to: "components/qa/answer-form.tsx"
      via: "import AnswerForm, pass questionId prop (only when user authenticated)"
      pattern: "AnswerForm.*questionId"
    - from: "components/qa/answer-form.tsx"
      to: "app/qa/actions.ts createAnswer"
      via: "useFormState(createAnswer, null) and hidden input question_id"
      pattern: "useFormState.*createAnswer"
    - from: "components/qa/reply-form.tsx"
      to: "app/qa/actions.ts createReply"
      via: "useFormState(createReply, null) and hidden input answer_id"
      pattern: "useFormState.*createReply"
---

<objective>
Voeg Q&A-formulieren toe zodat ingelogde gebruikers vragen, antwoorden en reacties kunnen plaatsen. Niet-ingelogde gebruikers zien een login-CTA op elke plek waar een formulier zou staan.

Purpose: Completeert de Q&A-cyclus — lezen (Phase 15) plus schrijven (Phase 16) — zodat de community-functionaliteit bruikbaar is.
Output: Vier nieuwe/gewijzigde bestanden: nieuwe-vraag/page.tsx, ask-question-form.tsx, answer-form.tsx, reply-form.tsx; plus bijgewerkte vraag-detailpagina en CHANGELOG.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-qa-frontend/15-01-SUMMARY.md
@app/qa/actions.ts
@app/qa/page.tsx
@app/qa/vraag/[id]/page.tsx
@lib/qa/types.ts
@lib/supabase/server.ts
@components/ui/button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: AskQuestionForm — /qa/nieuwe-vraag pagina + Client Component formulier</name>
  <files>
    app/qa/nieuwe-vraag/page.tsx
    components/qa/ask-question-form.tsx
  </files>
  <action>
Maak twee bestanden aan.

**`app/qa/nieuwe-vraag/page.tsx`** — Server Component, controleert auth, rendert login-CTA of het formulier:

```typescript
import type { Metadata } from 'next'
import Link from 'next/link'
import { createClient } from '@/lib/supabase/server'
import { AskQuestionForm } from '@/components/qa/ask-question-form'

export const metadata: Metadata = {
  title: 'Vraag stellen',
  description: 'Stel een vraag aan de AI Assistenten Hub community.',
}

export default async function NieuweVraagPage() {
  const supabase = await createClient()
  const {
    data: { user },
  } = await supabase.auth.getUser()

  if (!user) {
    return (
      <div className="container mx-auto px-4 py-8 max-w-2xl">
        <h1 className="text-4xl font-bold mb-6 text-foreground">Vraag stellen</h1>
        <div className="bg-muted rounded-lg p-6 text-center">
          <p className="text-muted-foreground mb-4">
            Je moet ingelogd zijn om een vraag te stellen.
          </p>
          <Link
            href="/login"
            className="inline-flex items-center justify-center rounded-md bg-primary px-4 py-2 text-sm font-medium text-primary-foreground hover:bg-primary/90"
          >
            Inloggen
          </Link>
        </div>
      </div>
    )
  }

  return (
    <div className="container mx-auto px-4 py-8 max-w-2xl">
      <h1 className="text-4xl font-bold mb-6 text-foreground">Vraag stellen</h1>
      <AskQuestionForm />
    </div>
  )
}
```

**`components/qa/ask-question-form.tsx`** — Client Component met `useFormState` (react-dom, React 18.3.1) en `useRouter` voor redirect na succes:

```typescript
'use client'

import { useFormState } from 'react-dom'
import { useEffect, useRef } from 'react'
import { useRouter } from 'next/navigation'
import { createQuestion } from '@/app/qa/actions'
import { Button } from '@/components/ui/button'

type FormState = { error?: string; id?: string } | null

export function AskQuestionForm() {
  const router = useRouter()
  const [state, formAction] = useFormState<FormState, FormData>(createQuestion, null)
  const formRef = useRef<HTMLFormElement>(null)

  useEffect(() => {
    if (state?.id) {
      router.push(`/qa/vraag/${state.id}`)
    }
  }, [state, router])

  return (
    <form ref={formRef} action={formAction} className="flex flex-col gap-6">
      {state?.error && (
        <div className="text-red-600 bg-red-50 rounded-md px-4 py-3 text-sm" role="alert">
          {state.error}
        </div>
      )}

      <div className="flex flex-col gap-2">
        <label htmlFor="title" className="font-medium text-foreground">
          Titel
        </label>
        <input
          id="title"
          name="title"
          type="text"
          required
          minLength={10}
          placeholder="Minimaal 10 tekens"
          className="border border-input rounded-md px-3 py-2 w-full bg-background text-foreground placeholder:text-muted-foreground focus:outline-none focus:ring-2 focus:ring-ring"
        />
      </div>

      <div className="flex flex-col gap-2">
        <label htmlFor="body" className="font-medium text-foreground">
          Beschrijving
        </label>
        <textarea
          id="body"
          name="body"
          required
          minLength={20}
          rows={6}
          placeholder="Minimaal 20 tekens. Beschrijf je vraag zo duidelijk mogelijk."
          className="border border-input rounded-md px-3 py-2 w-full bg-background text-foreground placeholder:text-muted-foreground focus:outline-none focus:ring-2 focus:ring-ring resize-y"
        />
      </div>

      <div className="flex gap-3">
        <Button type="submit">Vraag stellen</Button>
        <Button type="button" variant="outline" onClick={() => router.back()}>
          Annuleren
        </Button>
      </div>
    </form>
  )
}
```

Opmerking: `useFormState` is geimporteerd uit `react-dom` (niet uit `react`) conform beslissing 05-01-D3. Het type van `useFormState` is `useFormState<S, P>(action, initialState)` waarbij `action: (state: S, payload: P) => S | Promise<S>`. De type-annotatie `useFormState<FormState, FormData>` werkt correct met de bestaande `createQuestion(formData: FormData)` signature.
  </action>
  <verify>
    - `npx tsc --noEmit` passeert zonder errors in ask-question-form.tsx en nieuwe-vraag/page.tsx
    - Controleer dat `components/qa/ask-question-form.tsx` begint met `'use client'`
    - Controleer dat `app/qa/nieuwe-vraag/page.tsx` GEEN `'use client'` bevat
    - Grep: `grep -n "useFormState" components/qa/ask-question-form.tsx` toont import uit `react-dom`
    - Grep: `grep -n "createQuestion" components/qa/ask-question-form.tsx` toont import uit `@/app/qa/actions`
    - Grep: `grep -n "router.push" components/qa/ask-question-form.tsx` toont redirect op `state?.id`
  </verify>
  <done>
    - `app/qa/nieuwe-vraag/page.tsx` bestaat en bevat `createClient().auth.getUser()` check
    - Niet-ingelogde gebruiker: pagina toont login-CTA met link naar `/login`
    - Ingelogde gebruiker: pagina toont `AskQuestionForm` component
    - `components/qa/ask-question-form.tsx` bestaat met `'use client'`, `useFormState(createQuestion, null)`, en `useEffect` redirect op `state.id`
    - TypeScript compileert foutloos
  </done>
</task>

<task type="auto">
  <name>Task 2: AnswerForm + ReplyForm — integratie op de Q&A-detailpagina</name>
  <files>
    components/qa/answer-form.tsx
    components/qa/reply-form.tsx
    app/qa/vraag/[id]/page.tsx
    app/qa/page.tsx
  </files>
  <action>
Maak twee Client Components en update de detailpagina.

**`components/qa/answer-form.tsx`** — Client Component voor antwoorden:

```typescript
'use client'

import { useFormState } from 'react-dom'
import { useRef } from 'react'
import { createAnswer } from '@/app/qa/actions'
import { Button } from '@/components/ui/button'

type FormState = { error?: string; id?: string } | null

interface AnswerFormProps {
  questionId: string
}

export function AnswerForm({ questionId }: AnswerFormProps) {
  const [state, formAction] = useFormState<FormState, FormData>(createAnswer, null)
  const formRef = useRef<HTMLFormElement>(null)

  // Reset textarea after successful submit
  if (state?.id && formRef.current) {
    formRef.current.reset()
  }

  return (
    <form ref={formRef} action={formAction} className="flex flex-col gap-4 mt-6">
      <h2 className="text-xl font-semibold text-foreground">Plaats een antwoord</h2>

      {state?.error && (
        <div className="text-red-600 bg-red-50 rounded-md px-4 py-3 text-sm" role="alert">
          {state.error}
        </div>
      )}

      {state?.id && (
        <div className="text-green-700 bg-green-50 rounded-md px-4 py-3 text-sm" role="status">
          Antwoord geplaatst!
        </div>
      )}

      <input type="hidden" name="question_id" value={questionId} />

      <div className="flex flex-col gap-2">
        <label htmlFor="answer-body" className="font-medium text-foreground">
          Antwoord
        </label>
        <textarea
          id="answer-body"
          name="body"
          required
          minLength={10}
          rows={5}
          placeholder="Minimaal 10 tekens"
          className="border border-input rounded-md px-3 py-2 w-full bg-background text-foreground placeholder:text-muted-foreground focus:outline-none focus:ring-2 focus:ring-ring resize-y"
        />
      </div>

      <div>
        <Button type="submit">Antwoord plaatsen</Button>
      </div>
    </form>
  )
}
```

**`components/qa/reply-form.tsx`** — Client Component voor reacties met show/hide toggle:

```typescript
'use client'

import { useFormState } from 'react-dom'
import { useState, useRef } from 'react'
import { createReply } from '@/app/qa/actions'
import { Button } from '@/components/ui/button'

type FormState = { error?: string; id?: string } | null

interface ReplyFormProps {
  answerId: string
}

export function ReplyForm({ answerId }: ReplyFormProps) {
  const [open, setOpen] = useState(false)
  const [state, formAction] = useFormState<FormState, FormData>(createReply, null)
  const formRef = useRef<HTMLFormElement>(null)

  // Reset and close after successful submit
  if (state?.id && open) {
    setOpen(false)
    formRef.current?.reset()
  }

  return (
    <div className="mt-2">
      {!open && (
        <Button
          type="button"
          variant="ghost"
          size="sm"
          onClick={() => setOpen(true)}
        >
          Reageer
        </Button>
      )}

      {open && (
        <form ref={formRef} action={formAction} className="flex flex-col gap-3 mt-2">
          {state?.error && (
            <div className="text-red-600 bg-red-50 rounded-md px-3 py-2 text-xs" role="alert">
              {state.error}
            </div>
          )}

          <input type="hidden" name="answer_id" value={answerId} />

          <div className="flex flex-col gap-1">
            <label htmlFor={`reply-body-${answerId}`} className="text-sm font-medium text-foreground">
              Reactie
            </label>
            <textarea
              id={`reply-body-${answerId}`}
              name="body"
              required
              minLength={5}
              rows={3}
              placeholder="Minimaal 5 tekens"
              className="border border-input rounded-md px-3 py-2 w-full bg-background text-foreground placeholder:text-muted-foreground focus:outline-none focus:ring-2 focus:ring-ring resize-y text-sm"
            />
          </div>

          <div className="flex gap-2">
            <Button type="submit" size="sm">Reactie plaatsen</Button>
            <Button
              type="button"
              variant="ghost"
              size="sm"
              onClick={() => { setOpen(false) }}
            >
              Annuleren
            </Button>
          </div>
        </form>
      )}
    </div>
  )
}
```

**Update `app/qa/vraag/[id]/page.tsx`** — voeg auth check toe en integreer AnswerForm + ReplyForm. Vervang het volledige bestand:

```typescript
import type { Metadata } from 'next'
import { notFound } from 'next/navigation'
import Link from 'next/link'
import { createClient } from '@/lib/supabase/server'
import { getQuestionById, getAnswersByQuestionId, getRepliesByAnswerId } from '@/lib/qa/queries'
import { AnswerForm } from '@/components/qa/answer-form'
import { ReplyForm } from '@/components/qa/reply-form'

type Props = { params: Promise<{ id: string }> }

export async function generateMetadata({ params }: Props): Promise<Metadata> {
  const { id } = await params
  const question = await getQuestionById(id)
  if (!question) {
    return { title: 'Vraag niet gevonden' }
  }
  return {
    title: question.title,
    description: `Bekijk de vraag "${question.title}" en antwoorden van de AI Assistenten community.`,
  }
}

export default async function QAVraagPage({ params }: Props) {
  const { id } = await params

  const supabase = await createClient()
  const {
    data: { user },
  } = await supabase.auth.getUser()

  const question = await getQuestionById(id)
  if (!question) notFound()

  const answers = await getAnswersByQuestionId(id)

  const answersWithReplies = await Promise.all(
    answers.map(async (answer) => ({
      ...answer,
      replies: await getRepliesByAnswerId(answer.id),
    }))
  )

  return (
    <div className="container mx-auto px-4 py-8">
      {/* Question */}
      <div className="mb-8">
        <h1 className="text-4xl font-bold mb-4 text-foreground">{question.title}</h1>
        <p className="text-lg text-foreground mb-4">{question.body}</p>
        <p className="text-sm text-muted-foreground">
          Door {question.profiles?.display_name ?? 'Gebruiker'} &middot;{' '}
          {new Date(question.created_at).toLocaleDateString('nl-NL')}
        </p>
      </div>

      {/* Answers */}
      <div>
        <h2 className="text-2xl font-semibold mb-4 text-foreground">
          Antwoorden ({answersWithReplies.length})
        </h2>
        {answersWithReplies.length === 0 ? (
          <p className="text-muted-foreground">Nog geen antwoorden. Wees de eerste!</p>
        ) : (
          <ul className="space-y-6">
            {answersWithReplies.map((answer) => (
              <li key={answer.id} className="bg-muted rounded-lg p-4">
                <p className="text-foreground mb-2">{answer.body}</p>
                <p className="text-sm text-muted-foreground mb-4">
                  Door {answer.profiles?.display_name ?? 'Gebruiker'} &middot;{' '}
                  {new Date(answer.created_at).toLocaleDateString('nl-NL')}
                </p>

                {/* Replies */}
                {answer.replies.length > 0 && (
                  <ul className="space-y-2 border-l-2 border-border pl-4 mt-2">
                    {answer.replies.map((reply) => (
                      <li key={reply.id} className="text-sm">
                        <p className="text-foreground">{reply.body}</p>
                        <p className="text-xs text-muted-foreground mt-1">
                          Door {reply.profiles?.display_name ?? 'Gebruiker'} &middot;{' '}
                          {new Date(reply.created_at).toLocaleDateString('nl-NL')}
                        </p>
                      </li>
                    ))}
                  </ul>
                )}

                {/* Reply form or login CTA */}
                {user ? (
                  <ReplyForm answerId={answer.id} />
                ) : (
                  <div className="mt-3">
                    <Link
                      href="/login"
                      className="text-sm text-primary hover:underline"
                    >
                      Inloggen om te reageren
                    </Link>
                  </div>
                )}
              </li>
            ))}
          </ul>
        )}
      </div>

      {/* Answer form or login CTA */}
      <div className="mt-8 border-t border-border pt-8">
        {user ? (
          <AnswerForm questionId={id} />
        ) : (
          <div className="bg-muted rounded-lg p-6 text-center">
            <p className="text-muted-foreground mb-3">
              Je moet ingelogd zijn om een antwoord te plaatsen.
            </p>
            <Link
              href="/login"
              className="inline-flex items-center justify-center rounded-md bg-primary px-4 py-2 text-sm font-medium text-primary-foreground hover:bg-primary/90"
            >
              Inloggen
            </Link>
          </div>
        )}
      </div>
    </div>
  )
}
```

Belangrijke details:
- `AnswerForm` en `ReplyForm` zijn Client Components maar worden gerenderd vanuit een Server Component. Next.js laat dit toe; de serialiseerbare props (`questionId: string`, `answerId: string`) worden doorgegeven als gewone strings.
- `createAnswer` roept al `revalidatePath('/qa/vraag/${question_id}')` aan in actions.ts — na submit refresht Next.js de Server Component automatisch, waardoor het nieuwe antwoord zichtbaar wordt.
- `createReply` roept `revalidatePath('/qa')` aan. In Next.js App Router revalideert dit alle geneste routes inclusief `/qa/vraag/[id]`, zodat de nieuwe reactie na submit zichtbaar wordt. Geen wijzigingen aan actions.ts nodig.

Voeg ook een "Vraag stellen" link toe aan `app/qa/page.tsx` zodat ingelogde gebruikers makkelijk naar `/qa/nieuwe-vraag` kunnen navigeren. Update de QAPage:

```typescript
// Voeg toe boven de questions lijst, na de intro-tekst:
<div className="mb-6">
  <Link
    href="/qa/nieuwe-vraag"
    className="inline-flex items-center justify-center rounded-md bg-primary px-4 py-2 text-sm font-medium text-primary-foreground hover:bg-primary/90"
  >
    Vraag stellen
  </Link>
</div>
```

Voeg deze Link ook toe aan `app/qa/page.tsx` na de `<p className="text-lg text-muted-foreground mb-8">` paragraaf. Dit is een eenvoudige link, geen auth-check nodig — niet-ingelogde gebruikers worden op /qa/nieuwe-vraag zelf doorgestuurd naar login.

Let op: `app/qa/page.tsx` wordt ook gewijzigd (voeg Link import en knop toe). Voeg dit toe aan `files_modified`.
  </action>
  <verify>
    - `npx tsc --noEmit` passeert voor alle drie nieuwe/gewijzigde bestanden
    - Grep: `grep -n "'use client'" components/qa/answer-form.tsx` toont regel 1
    - Grep: `grep -n "'use client'" components/qa/reply-form.tsx` toont regel 1
    - Grep: `grep -n "useFormState" components/qa/answer-form.tsx` toont import uit `react-dom`
    - Grep: `grep -n "useFormState" components/qa/reply-form.tsx` toont import uit `react-dom`
    - Grep: `grep -n "question_id" components/qa/answer-form.tsx` toont hidden input
    - Grep: `grep -n "answer_id" components/qa/reply-form.tsx` toont hidden input
    - Grep: `grep -n "AnswerForm\|ReplyForm" app/qa/vraag/[id]/page.tsx` toont beide imports en gebruik
    - Grep: `grep -n "getUser" app/qa/vraag/[id]/page.tsx` toont auth check
    - Grep: `grep -n "Vraag stellen" app/qa/page.tsx` toont de link naar /qa/nieuwe-vraag
  </verify>
  <done>
    - `components/qa/answer-form.tsx` bestaat, heeft `'use client'`, accepteert `questionId: string` prop, heeft hidden input `question_id`, toont error bij `state.error` en succesmelding bij `state.id`
    - `components/qa/reply-form.tsx` bestaat, heeft `'use client'`, accepteert `answerId: string` prop, toont toggle knop "Reageer", toont inline formulier na klikken, sluit na succesvolle submit
    - `app/qa/vraag/[id]/page.tsx` bevat `createClient().auth.getUser()`, rendert `AnswerForm` voor ingelogde gebruiker en login-CTA voor niet-ingelogde gebruiker, rendert `ReplyForm` per antwoord voor ingelogde gebruiker
    - `app/qa/page.tsx` bevat link naar `/qa/nieuwe-vraag`
    - TypeScript compileert foutloos
  </done>
</task>

<task type="auto">
  <name>Task 3: CHANGELOG bijwerken voor Phase 16</name>
  <files>docs/CHANGELOG.md</files>
  <action>
Voeg een nieuwe sectie toe bovenaan `docs/CHANGELOG.md` (boven de `## [Phase 15]` sectie):

```markdown
## [Phase 16] - 2026-02-10 - Q&A Formulieren

### Added
- **Pagina "Vraag stellen"** (`app/qa/nieuwe-vraag/page.tsx`): Server Component met auth-check; niet-ingelogde gebruikers zien login-CTA, ingelogde gebruikers zien `AskQuestionForm`
- **`AskQuestionForm`** (`components/qa/ask-question-form.tsx`): Client Component met `useFormState(createQuestion)`, foutafhandeling, en `useRouter`-redirect naar `/qa/vraag/[id]` na succesvolle submit
- **`AnswerForm`** (`components/qa/answer-form.tsx`): Client Component met `useFormState(createAnswer)`, hidden `question_id` input, inline error- en successtatus
- **`ReplyForm`** (`components/qa/reply-form.tsx`): Client Component met toggle (Reageer-knop → formulier → sluit na submit), `useFormState(createReply)`, hidden `answer_id` input
- **Auth-gate op detailpagina** (`app/qa/vraag/[id]/page.tsx`): `createClient().auth.getUser()` bepaalt of `AnswerForm`/`ReplyForm` of login-CTA getoond wordt
- **"Vraag stellen" knop op Q&A-overzicht** (`app/qa/page.tsx`): Link naar `/qa/nieuwe-vraag` zodat gebruikers makkelijk een vraag kunnen stellen

### Technical Details
- `useFormState` geïmporteerd uit `react-dom` (React 18.3.1, conform beslissing 05-01-D3)
- Auth check via `createClient().auth.getUser()` in Server Components (conform beslissing 03-01-D3)
- Na `createQuestion` succes: `useEffect` op `state.id` triggert `router.push('/qa/vraag/${id}')` — geen redirect() in de Server Action nodig omdat de action al in een Client Component context wordt aangeroepen
- `createAnswer` en `createReply` doen al `revalidatePath` in actions.ts — na submit refresht Next.js de Server Component automatisch
- Alle formulieren gebruiken semantische Tailwind classes (`border-input`, `bg-background`, `text-foreground`)

---
```
  </action>
  <verify>
    - Grep: `grep -n "Phase 16" docs/CHANGELOG.md` toont de nieuwe sectie
    - De `## [Phase 15]` sectie is intact (niet overschreven)
  </verify>
  <done>
    - `docs/CHANGELOG.md` bevat `## [Phase 16]` als eerste sectie boven `## [Phase 15]`
    - Alle zes nieuwe/gewijzigde bestanden zijn vermeld in de changelog
  </done>
</task>

</tasks>

<verification>
Na het uitvoeren van alle drie taken:

1. TypeScript: `npx tsc --noEmit` — geen errors
2. Build: `npx next build` — geen build errors (optioneel, maar aanbevolen)
3. Bestandscontrole:
   - `ls app/qa/nieuwe-vraag/page.tsx` — bestaat
   - `ls components/qa/ask-question-form.tsx` — bestaat
   - `ls components/qa/answer-form.tsx` — bestaat
   - `ls components/qa/reply-form.tsx` — bestaat
4. Grep controles:
   - `grep -n "createClient" app/qa/nieuwe-vraag/page.tsx` — toont auth check
   - `grep -n "createClient" app/qa/vraag/[id]/page.tsx` — toont auth check
   - `grep -n "useFormState" components/qa/ask-question-form.tsx` — import uit react-dom
   - `grep -n "useFormState" components/qa/answer-form.tsx` — import uit react-dom
   - `grep -n "useFormState" components/qa/reply-form.tsx` — import uit react-dom
   - `grep -n "Phase 16" docs/CHANGELOG.md` — changelog entry aanwezig
</verification>

<success_criteria>
Phase 16 is compleet wanneer:
- [ ] `/qa/nieuwe-vraag` bestaat en toont login-CTA voor niet-ingelogde gebruikers
- [ ] `/qa/nieuwe-vraag` toont vraag-formulier voor ingelogde gebruikers
- [ ] Na succesvolle vraagstelling wordt de gebruiker doorgestuurd naar `/qa/vraag/[id]`
- [ ] Op `/qa/vraag/[id]` zien ingelogde gebruikers `AnswerForm` onderaan de pagina
- [ ] Op `/qa/vraag/[id]` zien ingelogde gebruikers `ReplyForm` per antwoord
- [ ] Niet-ingelogde gebruikers op detailpagina zien login-CTA's in plaats van formulieren
- [ ] Foutmeldingen van Server Actions (bijv. "Titel moet minimaal 10 tekens zijn") worden getoond
- [ ] `useFormState` is geïmporteerd uit `react-dom` (niet uit `react`)
- [ ] TypeScript compileert foutloos (`npx tsc --noEmit`)
- [ ] CHANGELOG bevat Phase 16 entry
</success_criteria>

<output>
Na voltooiing, maak `.planning/phases/16-qa-formulieren/16-01-SUMMARY.md` aan met:
- Frontmatter: phase, plan, status: complete, date, files_modified, decisions_made
- Samenvatting van wat gebouwd is
- Technische details (welke patterns gebruikt, welke beslissingen genomen)
- Eventuele afwijkingen van het plan
</output>
