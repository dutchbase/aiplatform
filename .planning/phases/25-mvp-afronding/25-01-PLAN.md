---
phase: 25-mvp-afronding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/MVP-CHECKLIST.md
  - README.md
autonomous: false

must_haves:
  truths:
    - "A docs/MVP-CHECKLIST.md file exists listing every PRD 11.1 must-have with a pass/fail/note per item"
    - "README.md reflects the actual project state: correct Node version, correct npm scripts, links to all docs/ files"
    - "User has confirmed (via checkpoint) that all critical checklist items pass on the live Vercel deployment"
  artifacts:
    - path: "docs/MVP-CHECKLIST.md"
      provides: "Written MVP verification checklist with pass/fail per PRD must-have item"
      contains: "OpenClaw"
    - path: "README.md"
      provides: "Updated README with correct technical details and documentation links"
      contains: "Node.js 24"
  key_links:
    - from: "docs/MVP-CHECKLIST.md"
      to: "app/openclaw/installatie/page.tsx"
      via: "checklist item references actual file"
      pattern: "installatie"
    - from: "README.md"
      to: "docs/"
      via: "links to docs/CHANGELOG.md, docs/database-schema.md, docs/seo.md, docs/qa.md, docs/rss.md, docs/blog.md, docs/supabase-setup.md"
      pattern: "docs/"
---

<objective>
Run a codebase-level MVP verification pass: read the actual source files to confirm every PRD 11.1 must-have is implemented, produce docs/MVP-CHECKLIST.md, update README.md with correct project details, then pause for the user to verify the live production site.

Purpose: Phase 25 is the final phase. This plan creates an auditable record of what is live, corrects the README, and gives the user a production sign-off checkpoint before the changelog release entry in Plan 02.
Output: docs/MVP-CHECKLIST.md, updated README.md, user-confirmed production sign-off.
</objective>

<execution_context>
@/home/dutchbase/.claude/get-shit-done/workflows/execute-plan.md
@/home/dutchbase/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/codebase/STACK.md
@.planning/codebase/ARCHITECTURE.md
@docs/CHANGELOG.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read codebase and produce docs/MVP-CHECKLIST.md</name>
  <files>docs/MVP-CHECKLIST.md</files>
  <action>
Read each file listed below and verify the stated condition holds. Then write docs/MVP-CHECKLIST.md with the results.

**Files to read and conditions to verify:**

1. `app/openclaw/installatie/page.tsx`
   - Contains real Dutch content (not a placeholder notice)
   - Has metadata export with a real title
   - Condition: PASS if file has `export const metadata` and no `placeholder` or `TODO` strings

2. `lib/data/tutorials.ts`
   - Exports a `tutorials` array with exactly 3 items
   - Each item has a non-empty `slug`, `title`, and `content.steps` array with at least 3 steps
   - Condition: PASS if 3 tutorials with ≥3 steps each

3. `app/qa/actions.ts`
   - Exports `createQuestion`, `createAnswer`, `createReply`
   - Each function checks auth via `supabase.auth.getUser()`
   - Condition: PASS if all three exported functions exist

4. `app/qa/vraag/[id]/page.tsx`
   - Renders answers and replies
   - Shows AnswerForm or login CTA depending on auth
   - Condition: PASS if file imports AnswerForm or references 'antwoord'

5. `app/login/actions.ts`
   - Exports `login` (sign in) and `signup` functions
   - Condition: PASS if both `login` and `signup` functions are present

6. `app/profiel/page.tsx`
   - Shows user profile (has auth check)
   - Condition: PASS if file references `supabase.auth.getUser` or `createClient`

7. `lib/data/blog.ts`
   - Exports `blogPosts` array with 3 items
   - Condition: PASS if array has exactly 3 entries

8. `app/feed.xml/route.ts`
   - Exports `GET` function
   - Returns RSS XML content
   - Condition: PASS if `GET` function exists and uses `application/rss+xml`

9. `app/sitemap.ts`
   - Exports a default sitemap function
   - Includes OpenClaw, blog, and Q&A paths
   - Condition: PASS if `/openclaw` and `/blog` and `/qa` appear in the URLs

10. `app/robots.ts`
    - Exports a robots function or `robots.txt` content
    - Condition: PASS if file exists and is non-empty

11. `components/ui/breadcrumbs.tsx`
    - Exists and exports Breadcrumbs component
    - Condition: PASS if file exists

12. `app/not-found.tsx`
    - Exists (custom 404 page)
    - Condition: PASS if file exists

13. `app/openclaw/tutorials/[slug]/page.tsx`
    - Uses `generateStaticParams` for SSG
    - Condition: PASS if `generateStaticParams` is present

Write the results to `docs/MVP-CHECKLIST.md` using this exact format:

```markdown
# MVP Checklist — Fase 25 Verificatie

**Datum:** 2026-02-11
**Doel:** Bevestig dat alle PRD 11.1 must-haves zijn geïmplementeerd vóór de MVP release.

---

## 1. OpenClaw sectie

| Item | Bestand | Status | Notitie |
|------|---------|--------|---------|
| Installatiepagina met echte inhoud | `app/openclaw/installatie/page.tsx` | PASS / FAIL | ... |
| 3 tutorials met echte content | `lib/data/tutorials.ts` | PASS / FAIL | ... |
| Tutorial detail met SSG | `app/openclaw/tutorials/[slug]/page.tsx` | PASS / FAIL | ... |

## 2. Q&A platform

| Item | Bestand | Status | Notitie |
|------|---------|--------|---------|
| createQuestion server action | `app/qa/actions.ts` | PASS / FAIL | ... |
| createAnswer server action | `app/qa/actions.ts` | PASS / FAIL | ... |
| createReply server action | `app/qa/actions.ts` | PASS / FAIL | ... |
| Q&A detailpagina met antwoorden | `app/qa/vraag/[id]/page.tsx` | PASS / FAIL | ... |

## 3. Accounts (auth)

| Item | Bestand | Status | Notitie |
|------|---------|--------|---------|
| Login (signIn) | `app/login/actions.ts` | PASS / FAIL | ... |
| Registratie (signUp) | `app/login/actions.ts` | PASS / FAIL | ... |
| Profielpagina | `app/profiel/page.tsx` | PASS / FAIL | ... |

## 4. Blog + RSS

| Item | Bestand | Status | Notitie |
|------|---------|--------|---------|
| 3 blogartikelen | `lib/data/blog.ts` | PASS / FAIL | ... |
| RSS feed endpoint | `app/feed.xml/route.ts` | PASS / FAIL | ... |

## 5. SEO structuur

| Item | Bestand | Status | Notitie |
|------|---------|--------|---------|
| Sitemap (inclusief OpenClaw, blog, Q&A) | `app/sitemap.ts` | PASS / FAIL | ... |
| Robots.txt | `app/robots.ts` | PASS / FAIL | ... |
| Breadcrumbs component | `components/ui/breadcrumbs.tsx` | PASS / FAIL | ... |
| 404 pagina | `app/not-found.tsx` | PASS / FAIL | ... |

---

## Samenvatting

- **Totaal items:** 13
- **PASS:** {N}
- **FAIL:** {N}
- **Opmerkingen:** {eventuele aandachtspunten}

---

*Codebase verificatie door Claude (plan 25-01). Productie verificatie door gebruiker in checkpoint.*
```

Fill in each Status cell with `PASS` or `FAIL` based on what you actually read. Fill in the Notitie cell with a brief note (e.g. "3 tutorials gevonden: eerste-stappen, configuratie, tips" or "function login en signup aanwezig").
  </action>
  <verify>Run: `test -f docs/MVP-CHECKLIST.md && echo "EXISTS"`. File exists with all 13 checklist items filled in. No cell says "PASS / FAIL" — every cell has a concrete status.</verify>
  <done>docs/MVP-CHECKLIST.md exists with 13 items, each showing PASS or FAIL with a brief note. Summary row shows totals.</done>
</task>

<task type="auto">
  <name>Task 2: Update README.md with accurate project details</name>
  <files>README.md</files>
  <action>
Read README.md first. It already has good content: project description, URL structure table, tech stack, roadmap overview, and local development instructions. Make these targeted updates:

**Update 1 — Node version:**
Change "Node.js 18+" in the Vereisten section to "Node.js 24+" (matching actual runtime in STACK.md: Node.js 24.12.0).

**Update 2 — Documentatie table:**
The current Documentatie table has 3 rows (prd.md, roadmap/ROADMAP.md, docs/CHANGELOG.md). Expand it to include all key docs/ files that were created during the build phases:

| Document | Beschrijving |
| -------- | ------------ |
| [prd.md](prd.md) | Product Requirements Document – doel, doelgroepen, architectuur, MVP, KPI's, risico's |
| [roadmap/ROADMAP.md](roadmap/ROADMAP.md) | Volledige roadmap met alle fases en links naar fase-documenten |
| [docs/CHANGELOG.md](docs/CHANGELOG.md) | Wijzigingslogboek per fase/release |
| [docs/database-schema.md](docs/database-schema.md) | Database schema, RLS policies en indexen (Supabase/Postgres) |
| [docs/supabase-setup.md](docs/supabase-setup.md) | Supabase project setup en configuratie |
| [docs/seo.md](docs/seo.md) | SEO-implementatie: metadata, sitemap, breadcrumbs, structured data |
| [docs/blog.md](docs/blog.md) | Blog data-structuur en instructies voor nieuwe artikelen |
| [docs/qa.md](docs/qa.md) | Q&A API-contract: server actions en query-functies |
| [docs/rss.md](docs/rss.md) | RSS feed implementatie en configuratie |

**Update 3 — MVP status badge/note:**
Add a short line after the opening tagline (before "## Wat is dit project?") to indicate MVP status:

```markdown
> **Status:** MVP live — 25 fases afgerond (februari 2026)
```

Do NOT rewrite or remove any other existing content. These are additive and corrective changes only.
  </action>
  <verify>Read the updated README.md and confirm: (1) "Node.js 24+" appears in Vereisten, (2) docs/database-schema.md appears in the Documentatie table, (3) the MVP status note appears near the top.</verify>
  <done>README.md has correct Node version (24+), expanded docs table with all 9 entries, and MVP status indicator. All existing content preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Task 1 produced docs/MVP-CHECKLIST.md with codebase-level verification of all 13 PRD 11.1 items.
    Task 2 updated README.md with correct Node version, expanded docs table, and MVP status.
  </what-built>
  <how-to-verify>
    **Step 1 — Review the checklist:**
    Open `docs/MVP-CHECKLIST.md` and confirm the summary shows mostly PASS items.
    If any item shows FAIL, note it — critical failures (auth, Q&A posting, tutorials) should be fixed before release; minor items (e.g. a missing doc file) can be noted as known gaps.

    **Step 2 — Verify production (Vercel):**
    Visit your Vercel deployment URL and test each area:
    1. `/openclaw/installatie` — page loads with real Dutch installation guide
    2. `/openclaw/tutorials/eerste-stappen` — tutorial renders with steps
    3. `/qa` — list of questions appears (or empty state message if no questions yet)
    4. `/qa/nieuwe-vraag` — shows login prompt or question form
    5. `/login` — login form works, signup link present
    6. `/blog` — 3 blog post cards visible
    7. `/feed.xml` — opens RSS XML in browser
    8. `/sitemap.xml` — opens sitemap XML with multiple URLs
    9. `/robots.txt` — opens robots rules

    **Step 3 — Check README:**
    Open README.md and confirm "Node.js 24+" and the expanded docs table are present.

    **Expected result:** All 9 production checks pass. Any issues seen? Note them.
  </how-to-verify>
  <resume-signal>Type "goedgekeurd" to proceed to Plan 02 (CHANGELOG release entry). Or describe any issues found so they can be addressed.</resume-signal>
</task>

</tasks>

<verification>
After tasks 1 and 2:
1. `test -f docs/MVP-CHECKLIST.md` — file exists
2. MVP-CHECKLIST.md contains all 5 sections (OpenClaw, Q&A, Accounts, Blog+RSS, SEO)
3. README.md contains "Node.js 24" in the Vereisten section
4. README.md docs table contains "docs/database-schema.md"
5. README.md contains MVP status note near the top
</verification>

<success_criteria>
- docs/MVP-CHECKLIST.md exists with 13 items, concrete PASS/FAIL per item, summary totals
- README.md is accurate: Node 24+, all docs/ files linked, MVP status visible
- User has reviewed the checklist and confirmed production site works via checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/25-mvp-afronding/25-01-SUMMARY.md` following the summary template at `@/home/dutchbase/.claude/get-shit-done/templates/summary.md`.
</output>
