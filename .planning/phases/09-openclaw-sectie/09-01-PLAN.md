---
phase: 09-openclaw-sectie
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/openclaw/page.tsx
  - app/openclaw/installatie/page.tsx
autonomous: true

must_haves:
  truths:
    - "User can access /openclaw and see OpenClaw overview with intro text"
    - "User can see navigation links to Installatie, Tutorials, Use cases, Nieuws"
    - "User can access /openclaw/installatie and see structured installation steps"
    - "Installation page has clear section headings ready for content"
    - "Both pages have proper Dutch metadata (title, description)"
  artifacts:
    - path: "app/openclaw/page.tsx"
      provides: "OpenClaw overview page with intro and navigation"
      min_lines: 40
    - path: "app/openclaw/installatie/page.tsx"
      provides: "Installation page with section structure"
      min_lines: 50
  key_links:
    - from: "app/openclaw/page.tsx"
      to: "components/ui/button.tsx"
      via: "Button import for navigation links"
      pattern: "import.*Button"
    - from: "app/openclaw/installatie/page.tsx"
      to: "app/layout.tsx"
      via: "Next.js metadata export"
      pattern: "export.*metadata"
---

<objective>
Create OpenClaw section foundation with overview page and installation page structure.

Purpose: Establish the OpenClaw section as a recognizable, navigable area of the site with clear structure ready for content population in Phase 19.

Output: Two functional pages with Dutch content, proper metadata, and semantic styling using existing design system.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@roadmap/phase-09-openclaw-sectie.md
@components/ui/button.tsx
@components/layout/header.tsx
@app/globals.css
@app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create OpenClaw overview page with intro and navigation</name>
  <files>app/openclaw/page.tsx</files>
  <action>
Create `app/openclaw/page.tsx` as Server Component with:

**Metadata:**
- title: "OpenClaw – AI Assistent voor Developers | AI Assistenten Hub"
- description: "Ontdek OpenClaw: de krachtige AI-assistent die je helpt met coderen, debugging en ontwikkeling. Installatie, tutorials en use cases."

**Content structure:**
1. **Hero section:**
   - H1: "OpenClaw – Je AI-Assistent voor Development"
   - Introductie paragraaf (3-4 zinnen in het Nederlands): Wat is OpenClaw, waarom gebruiken, wat kan het (coderen, debugging, productiviteit)

2. **Navigation grid (4 cards):**
   Use Button component with asChild pattern for each card:
   - **Installatie** → `/openclaw/installatie` (variant: default)
     - Icon or emoji: 📦
     - Short description: "Stap-voor-stap installatie en configuratie"
   - **Tutorials** → `/openclaw/tutorials` (variant: default)
     - Icon or emoji: 📚
     - Short description: "Leer OpenClaw gebruiken met praktische voorbeelden"
   - **Use Cases** → `/openclaw/use-cases` (variant: secondary)
     - Icon or emoji: 💡
     - Short description: "Ontdek wat je allemaal kunt doen met OpenClaw"
   - **Nieuws** → `/openclaw/nieuws` (variant: secondary)
     - Icon or emoji: 📰
     - Short description: "Updates en nieuwe features"

**Styling:**
- Use semantic token classes ONLY (bg-background, text-foreground, text-muted-foreground, border-border)
- Container class for responsive layout
- Grid layout: 2 columns on desktop (lg:grid-cols-2), 1 column on mobile
- Card-like appearance: border, rounded corners, padding, hover state
- Spacing: adequate padding and gaps for readability

**Implementation notes:**
- Server Component (no 'use client')
- Import Button from @/components/ui/button
- Import Link from next/link
- Use Button with asChild pattern: `<Button asChild><Link href="...">...</Link></Button>`
- NO placeholder text like "Coming soon" - all text should be substantive Dutch content
- NO hardcoded colors (text-gray-700, bg-blue-500) - semantic tokens only
  </action>
  <verify>
1. File exists: `app/openclaw/page.tsx`
2. Build succeeds: `npm run build` (no TypeScript errors)
3. Metadata exports title and description
4. Page renders with H1, intro paragraph, 4 navigation cards
5. All Button components use asChild pattern with Link
6. NO raw color classes (grep for text-gray, bg-blue, etc.)
  </verify>
  <done>
OpenClaw overview page exists at `/openclaw` with Dutch intro text, proper metadata, and 4 navigation cards using Button component with semantic tokens only.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create installation page with section structure</name>
  <files>app/openclaw/installatie/page.tsx</files>
  <action>
Create `app/openclaw/installatie/page.tsx` as Server Component with:

**Metadata:**
- title: "OpenClaw Installatie – Stap voor Stap | AI Assistenten Hub"
- description: "Installeer OpenClaw in enkele stappen: vereisten, installatie, configuratie en verificatie. Complete Nederlandse handleiding."

**Content structure:**

1. **Page header:**
   - H1: "OpenClaw Installatie"
   - Intro paragraph (2-3 zinnen): Wat je gaat installeren, geschatte tijd, wat je nodig hebt

2. **Section 1: Vereisten**
   - H2: "Vereisten"
   - Bullet list (placeholders for now, content in Phase 19):
     - "Node.js 18 of hoger"
     - "npm of yarn package manager"
     - "Git voor installatie via repository"
   - Note: "Deze sectie wordt verder uitgebreid in een latere fase."

3. **Section 2: Installatie**
   - H2: "Installatie"
   - Ordered list with numbered steps (3-4 steps):
     1. "Download OpenClaw via npm of git clone"
     2. "Installeer dependencies"
     3. "Configureer je API keys (indien nodig)"
   - Placeholder: "Gedetailleerde commando's en uitleg volgen in Phase 19."

4. **Section 3: Verificatie**
   - H2: "Controleren of het werkt"
   - Paragraph: "Na installatie kun je controleren of OpenClaw correct werkt door..."
   - Bullet list (placeholders):
     - "Test commando uitvoeren"
     - "Eerste vraag stellen"
   - Placeholder note: "Voorbeelden en screenshots volgen later."

5. **Next steps CTA:**
   - Paragraph: "OpenClaw succesvol geïnstalleerd? Ga verder met onze tutorials."
   - Button (variant: default): "Bekijk Tutorials" → `/openclaw/tutorials`

**Styling:**
- Use semantic token classes ONLY
- Container class for responsive layout
- Max-width for readability (prose or max-w-3xl)
- Proper spacing between sections (space-y-8 or similar)
- Lists should be styled clearly (list-disc, list-decimal)
- Button with asChild pattern for CTA

**Implementation notes:**
- Server Component (no 'use client')
- Import Button and Link
- Use semantic HTML (h1, h2, ul, ol, p)
- Content is structural placeholders, not fake detailed instructions
- Make it clear this is ready for content in Phase 19 (via placeholder notes)
  </action>
  <verify>
1. File exists: `app/openclaw/installatie/page.tsx`
2. Build succeeds: `npm run build`
3. Metadata exports title and description
4. Page has H1, intro, 3 H2 sections (Vereisten, Installatie, Verificatie)
5. Sections use lists (ul for requirements, ol for steps)
6. CTA Button with asChild pattern links to /openclaw/tutorials
7. NO raw color classes
  </verify>
  <done>
Installation page exists at `/openclaw/installatie` with clear section structure (Vereisten, Installatie, Verificatie), proper metadata, and semantic styling. Structure is ready for content population in Phase 19.
  </done>
</task>

</tasks>

<verification>
**Automated checks:**
1. Both files exist in correct locations
2. Build passes: `npm run build`
3. No TypeScript errors
4. Both pages export metadata
5. Button component imported and used correctly
6. No raw color classes (semantic tokens only)

**Manual checks:**
1. Visit http://localhost:3000/openclaw - overview page displays correctly
2. Click navigation cards - links work
3. Visit http://localhost:3000/openclaw/installatie - installation page displays
4. Verify mobile responsiveness (grid collapses to 1 column)
5. Check dark mode appearance
</verification>

<success_criteria>
- `/openclaw` displays overview with intro and 4 navigation cards
- `/openclaw/installatie` displays structured installation guide with 3 sections
- Both pages use Dutch content, proper metadata, semantic tokens
- Navigation links work (even if target pages don't have content yet)
- Build passes clean
- No hardcoded colors or placeholder text like "Coming soon"
</success_criteria>

<output>
After completion, create `.planning/phases/09-openclaw-sectie/09-01-SUMMARY.md`
</output>
