---
phase: 15-qa-frontend
plan: 01
type: execute
wave: 1
depends_on: [14-01]
files_modified:
  - app/qa/page.tsx
  - app/qa/vraag/[id]/page.tsx
  - docs/CHANGELOG.md
autonomous: true

must_haves:
  truths:
    - "Bezoeker ziet op /qa een lijst van vragen met titel, auteur en datum (of een vriendelijke melding bij 0 vragen)"
    - "Bezoeker kan op een vraagtitel klikken en wordt doorgestuurd naar /qa/vraag/[id]"
    - "De detailpagina toont de vraag bovenaan, gevolgd door een gelabelde antwoordenlijst"
    - "Elk antwoord toont zijn eigen reacties visueel ingesprongen eronder"
    - "Een onbekend vraag-id levert een 404-pagina op via notFound()"
    - "De paginatitel van de detailpagina bevat de vraagtitel (niet alleen het id)"
    - "Alle tekst is in het Nederlands; geen 'use client' in deze bestanden"
  artifacts:
    - path: "app/qa/page.tsx"
      provides: "Q&A-overzichtspagina met vragenlijst en lege staat"
      exports: ["QAPage", "metadata"]
    - path: "app/qa/vraag/[id]/page.tsx"
      provides: "Vraag-detailpagina met antwoorden en reacties"
      exports: ["QAVraagPage", "generateMetadata"]
    - path: "lib/qa/queries.ts"
      provides: "Supabase leesquery's met profiles JOIN voor display_name"
      exports: ["getQuestions", "getQuestionById", "getAnswersByQuestionId", "getRepliesByAnswerId"]
  key_links:
    - from: "app/qa/page.tsx"
      to: "lib/qa/queries.ts"
      via: "import getQuestions"
      pattern: "getQuestions\\(\\)"
    - from: "app/qa/vraag/[id]/page.tsx"
      to: "lib/qa/queries.ts"
      via: "import getQuestionById, getAnswersByQuestionId, getRepliesByAnswerId"
      pattern: "getQuestionById\\(id\\)"
    - from: "app/qa/vraag/[id]/page.tsx"
      to: "next/navigation"
      via: "notFound() for unknown ids"
      pattern: "notFound\\(\\)"
---

<objective>
Replace the twee statische Q&A-placeholderpagina's met werkende weergaven die data ophalen uit Supabase via de queries uit Phase 14.

Purpose: Bezoekers kunnen vragen, antwoorden en reacties lezen voordat Phase 16 de formulieren toevoegt.
Output: Functionele Q&A-overzichts- en detailpagina met correcte lege/foutstates en dynamische metadata.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

@app/qa/page.tsx
@app/qa/vraag/[id]/page.tsx
@lib/qa/queries.ts
@lib/qa/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Q&A-overzichtspagina — vragenlijst met lege staat</name>
  <files>app/qa/page.tsx</files>
  <action>
Vervang de huidige placeholder in app/qa/page.tsx door een volledige Server Component die vragen ophaalt en weergeeft.

1. Importeer getQuestions uit lib/qa/queries.ts en het Question-type uit lib/qa/types.ts. Importeer Link uit next/link.

2. Houd de bestaande statische metadata (title: 'Q&A', description: '...'). Maak de paginafunctie async.

3. Roep `const questions = await getQuestions()` aan.

4. Als questions.length === 0: toon een vriendelijk bericht:
   ```
   <p className="text-lg text-muted-foreground">
     Nog geen vragen gesteld. Kom binnenkort terug!
   </p>
   ```

5. Anders: render een lijst van kaarten. Gebruik ul/li-structuur. Elke kaart:
   ```
   <li className="bg-muted rounded-lg p-4">
     <Link href={`/qa/vraag/${question.id}`} className="text-xl font-semibold text-foreground hover:underline">
       {question.title}
     </Link>
     <p className="text-sm text-muted-foreground mt-1">
       Door {question.profiles?.display_name ?? 'Gebruiker'} ·{' '}
       {new Date(question.created_at).toLocaleDateString('nl-NL')}
     </p>
   </li>
   ```

6. Wikkel de lijst in:
   ```
   <ul className="space-y-4">
   ```

7. Volledige paginastructuur:
   ```
   <div className="container mx-auto px-4 py-8">
     <h1 className="text-4xl font-bold mb-6 text-foreground">Q&A</h1>
     <p className="text-lg text-muted-foreground mb-8">
       Welkom bij onze Q&A community. Stel vragen, deel kennis en help elkaar met AI-assistenten.
     </p>
     {/* question list or empty state */}
   </div>
   ```

BELANGRIJK: Geen 'use client'. Geen hardcoded data. Het Question-type vanuit Phase 14 heeft mogelijk de shape `{ id: string, title: string, created_at: string, profiles: { display_name: string | null } | null }` vanwege de Supabase JOIN. Als de query in lib/qa/queries.ts nog geen profiles JOIN heeft, pas dan getQuestions() aldaar aan om `.select('*, profiles(display_name)')` te gebruiken en update het return type zodat het `profiles: { display_name: string | null } | null` bevat. Verander NIET de functiesignatuur — alleen de select-string en het returntype.
  </action>
  <verify>
    Voer `pnpm build` uit (of `pnpm type-check` als build te lang duurt). Navigeer naar /qa in dev-server. Controleer dat de pagina laadt zonder TypeScript-fouten.
  </verify>
  <done>
    app/qa/page.tsx is een async Server Component. Met data: toont vraagtitels als klikbare links naar /qa/vraag/[id], plus auteur en datum. Zonder data: toont vriendelijke melding. Geen 'use client'. TypeScript compileert zonder errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Vraag-detailpagina — vraag, antwoorden en reacties</name>
  <files>app/qa/vraag/[id]/page.tsx</files>
  <action>
Vervang de placeholder in app/qa/vraag/[id]/page.tsx door een volledige Server Component die de volledige vraag-thread weergeeft.

TYPE DEFINITIE:
```
type Props = { params: Promise<{ id: string }> }
```

GENERATEMETADATA (vervang bestaande):
```typescript
export async function generateMetadata({ params }: Props): Promise<Metadata> {
  const { id } = await params
  const question = await getQuestionById(id)
  if (!question) {
    return { title: 'Vraag niet gevonden' }
  }
  return {
    title: question.title,
    description: `Bekijk de vraag "${question.title}" en antwoorden van de AI Assistenten community.`,
  }
}
```

PAGINAFUNCTIE:
```typescript
export default async function QAVraagPage({ params }: Props) {
  const { id } = await params
  const question = await getQuestionById(id)
  if (!question) notFound()

  const answers = await getAnswersByQuestionId(id)

  // For each answer, fetch replies
  const answersWithReplies = await Promise.all(
    answers.map(async (answer) => ({
      ...answer,
      replies: await getRepliesByAnswerId(answer.id),
    }))
  )

  return (
    <div className="container mx-auto px-4 py-8">
      {/* Question */}
      <div className="mb-8">
        <h1 className="text-4xl font-bold mb-4 text-foreground">{question.title}</h1>
        <p className="text-lg text-foreground mb-4">{question.body}</p>
        <p className="text-sm text-muted-foreground">
          Door {question.profiles?.display_name ?? 'Gebruiker'} ·{' '}
          {new Date(question.created_at).toLocaleDateString('nl-NL')}
        </p>
      </div>

      {/* Answers */}
      <div>
        <h2 className="text-2xl font-semibold mb-4 text-foreground">
          Antwoorden ({answersWithReplies.length})
        </h2>
        {answersWithReplies.length === 0 ? (
          <p className="text-muted-foreground">Nog geen antwoorden. Wees de eerste!</p>
        ) : (
          <ul className="space-y-6">
            {answersWithReplies.map((answer) => (
              <li key={answer.id} className="bg-muted rounded-lg p-4">
                <p className="text-foreground mb-2">{answer.body}</p>
                <p className="text-sm text-muted-foreground mb-4">
                  Door {answer.profiles?.display_name ?? 'Gebruiker'} ·{' '}
                  {new Date(answer.created_at).toLocaleDateString('nl-NL')}
                </p>

                {/* Replies */}
                {answer.replies.length > 0 && (
                  <ul className="space-y-2 border-l-2 border-border pl-4 mt-2">
                    {answer.replies.map((reply) => (
                      <li key={reply.id} className="text-sm">
                        <p className="text-foreground">{reply.body}</p>
                        <p className="text-xs text-muted-foreground mt-1">
                          Door {reply.profiles?.display_name ?? 'Gebruiker'} ·{' '}
                          {new Date(reply.created_at).toLocaleDateString('nl-NL')}
                        </p>
                      </li>
                    ))}
                  </ul>
                )}
              </li>
            ))}
          </ul>
        )}
      </div>
    </div>
  )
}
```

IMPORTS BOVENAAN:
```typescript
import type { Metadata } from 'next'
import { notFound } from 'next/navigation'
import { getQuestionById, getAnswersByQuestionId, getRepliesByAnswerId } from '@/lib/qa/queries'
```

BELANGRIJK over de Supabase queries: Net als bij Task 1, als getAnswersByQuestionId en getRepliesByAnswerId nog geen profiles JOIN hebben, pas dan lib/qa/queries.ts aan om `.select('*, profiles(display_name)')` te gebruiken voor antwoorden en reacties. Het type van de returnwaarde moet `profiles: { display_name: string | null } | null` bevatten. Pas alleen de SELECT-strings en returntypes aan — niet de functiesignaturen.

Zorg dat `question.body` bestaat in het Question-type (Phase 14 schema). Als het veld `content` heet in plaats van `body`, gebruik dan `question.content`. Gebruik het veld dat in het daadwerkelijke schema staat.
  </action>
  <verify>
    Voer `pnpm build` uit of `pnpm type-check`. Navigeer in dev naar /qa/vraag/test-niet-bestaand — moet 404 tonen. Navigeer naar een bestaand id (of test met Supabase test-data) — moet vraag + antwoorden tonen.
  </verify>
  <done>
    app/qa/vraag/[id]/page.tsx is een async Server Component. generateMetadata geeft vraagtitel terug. Onbekend id roept notFound() aan. Bekende vraag toont: vraagtitel, vraagtekst, auteur/datum, antwoordenlijst met aantal, per antwoord ingesprongen reacties. Geen 'use client'. TypeScript compileert zonder errors.
  </done>
</task>

<task type="auto">
  <name>Task 3: CHANGELOG bijwerken voor Phase 15</name>
  <files>docs/CHANGELOG.md</files>
  <action>
Voeg een nieuwe sectie toe bovenaan in docs/CHANGELOG.md (na de eerste header-regels, voor de bestaande ## [Phase 7] entry):

```markdown
## [Phase 15] - 2026-02-10 - Q&A Frontend

### Added
- **Q&A-overzichtspagina** (`app/qa/page.tsx`): haalt vragen op via `getQuestions()` uit lib/qa/queries.ts; toont vraagtitels als links naar detailpagina's, auteur en datum
- **Q&A-detailpagina** (`app/qa/vraag/[id]/page.tsx`): toont volledige vraag-thread met vraag, antwoorden en per-antwoord reacties in visuele hiëarchie
- **Lege en foutstates**: overzicht toont vriendelijke melding bij 0 vragen; detail roept `notFound()` aan bij onbekend id
- **Dynamische metadata**: detailpagina's gebruiken vraagtitel in paginatitel via `generateMetadata`

### Changed
- `lib/qa/queries.ts`: queries bijgewerkt met `profiles(display_name)` JOIN voor auteursnamen
- `app/qa/page.tsx` en `app/qa/vraag/[id]/page.tsx`: vervangen door werkende implementaties

### Technical Details
- Server Components throughout — geen 'use client'
- `Promise.all` voor parallelle reactie-ophaling per antwoord
- Fallback `'Gebruiker'` voor anonieme profielen
- `toLocaleDateString('nl-NL')` voor Nederlandse datumnotatie
- `notFound()` from `next/navigation` voor 404-afhandeling

---

```
  </action>
  <verify>
    Lees docs/CHANGELOG.md en controleer dat de Phase 15-sectie bovenaan staat en alle punten correct zijn.
  </verify>
  <done>
    docs/CHANGELOG.md bevat een ## [Phase 15] sectie met Added, Changed en Technical Details. Geen bestaande inhoud verwijderd of gewijzigd.
  </done>
</task>

</tasks>

<verification>
Na alle drie taken:
1. `pnpm build` slaagt zonder TypeScript-fouten
2. `/qa` toont een vragenlijst of de lege-staat melding
3. `/qa/vraag/[geldig-id]` toont vraag + antwoorden + reacties
4. `/qa/vraag/niet-bestaand-id` geeft 404
5. Browser-tabbladtitel op detailpagina bevat de vraagtitel
6. Geen 'use client' in page-bestanden
7. CHANGELOG bevat Phase 15-entry
</verification>

<success_criteria>
- app/qa/page.tsx: async Server Component die getQuestions() aanroept, vraagenlijst of lege staat toont
- app/qa/vraag/[id]/page.tsx: async Server Component met generateMetadata, notFound() guard, visuele hiëarchie vraag > antwoorden > reacties
- lib/qa/queries.ts: queries retourneren profiles.display_name via JOIN (indien niet al aanwezig)
- Alle tekst in het Nederlands
- TypeScript strict-mode compileert zonder errors
- docs/CHANGELOG.md bijgewerkt
</success_criteria>

<output>
Na voltooiing, maak `.planning/phases/15-qa-frontend/15-01-SUMMARY.md` aan met de standaard samenvatting van wat is gebouwd, genomen beslissingen en verificatieresultaten.
</output>
