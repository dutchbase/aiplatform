---
phase: 10-openclaw-tutorials
plan: 01
type: execute
wave: 1
depends_on: [09-01, 09-02]
files_modified:
  - lib/data/tutorials.ts
  - app/openclaw/tutorials/page.tsx
  - app/openclaw/tutorials/[slug]/page.tsx
autonomous: true

must_haves:
  truths:
    - "User can access /openclaw/tutorials and see list of 3 tutorials"
    - "Each tutorial card shows title, description, and link to detail page"
    - "User can click tutorial link and reach detail page via slug"
    - "Detail pages support three slugs: eerste-stappen, configuratie, tips"
    - "Unknown slug redirects to tutorials overview or shows 404"
    - "Each page has unique Dutch metadata (title, description)"
  artifacts:
    - path: "lib/data/tutorials.ts"
      provides: "Typed tutorials data array with 3 tutorial entries"
      min_lines: 30
    - path: "app/openclaw/tutorials/page.tsx"
      provides: "Tutorials overview page listing all tutorials"
      min_lines: 40
    - path: "app/openclaw/tutorials/[slug]/page.tsx"
      provides: "Dynamic tutorial detail page with generateStaticParams"
      min_lines: 60
  key_links:
    - from: "app/openclaw/tutorials/page.tsx"
      to: "lib/data/tutorials.ts"
      via: "Import tutorials array"
      pattern: "import.*tutorials"
    - from: "app/openclaw/tutorials/[slug]/page.tsx"
      to: "lib/data/tutorials.ts"
      via: "Import tutorials for lookup and generateStaticParams"
      pattern: "import.*tutorials"
    - from: "app/openclaw/tutorials/[slug]/page.tsx"
      to: "Next.js dynamic routes"
      via: "generateStaticParams export"
      pattern: "export.*generateStaticParams"
---

<objective>
Create OpenClaw tutorials section with overview page and three tutorial detail pages accessible via slug-based routing.

Purpose: Establish tutorials structure ready for content population in Phase 19, enabling users to discover and access OpenClaw learning resources.

Output: Functional tutorials list and detail pages with proper metadata, routing, and semantic styling.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@roadmap/phase-10-openclaw-tutorials.md
@components/ui/button.tsx
@app/layout.tsx
@app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tutorials data structure</name>
  <files>lib/data/tutorials.ts</files>
  <action>
Create `lib/data/tutorials.ts` with typed tutorials data:

**Type definition:**
```typescript
export interface Tutorial {
  slug: string              // URL-safe identifier (eerste-stappen, configuratie, tips)
  title: string            // Display title in Dutch
  description: string      // Short description (2-3 sentences)
  lastUpdated: string      // Date string (YYYY-MM-DD format)
  content: {
    intro: string          // Introduction paragraph
    steps: Array<{
      heading: string      // Step heading
      content: string      // Step content (placeholder text)
    }>
  }
}
```

**Data array:**
Export constant array with 3 tutorials:

1. **eerste-stappen** (First Steps)
   - title: "Eerste Stappen met OpenClaw"
   - description: "Begin met OpenClaw: installatie, basis commando's en je eerste AI-gesprek."
   - lastUpdated: "2026-02-09"
   - content.intro: Paragraph explaining what first steps covers (placeholder, 3-4 sentences)
   - content.steps: 3 steps with headings and placeholder content
     - "OpenClaw starten"
     - "Je eerste vraag stellen"
     - "Code genereren met OpenClaw"

2. **configuratie** (Configuration)
   - title: "OpenClaw Configureren"
   - description: "Pas OpenClaw aan naar jouw workflow: instellingen, API keys en preferences."
   - lastUpdated: "2026-02-09"
   - content.intro: Paragraph explaining configuration options (placeholder, 3-4 sentences)
   - content.steps: 3 steps
     - "Basis instellingen"
     - "API keys configureren"
     - "Geavanceerde opties"

3. **tips** (Tips & Tricks)
   - title: "Tips en Trucs voor OpenClaw"
   - description: "Haal meer uit OpenClaw: productiviteitstips, shortcuts en best practices."
   - lastUpdated: "2026-02-09"
   - content.intro: Paragraph explaining tips focus (placeholder, 3-4 sentences)
   - content.steps: 4 tips
     - "Effectieve prompts schrijven"
     - "Gebruik van context"
     - "Keyboard shortcuts"
     - "Workflow optimalisatie"

**Implementation notes:**
- Export as named constant: `export const tutorials: Tutorial[] = [...]`
- All text in Dutch
- Placeholder content should be substantive but brief (not full tutorial text)
- Use present tense ("OpenClaw helpt je met..." not "OpenClaw zal helpen...")
- NO "Lorem ipsum" or "Coming soon" text
- Date format: ISO 8601 (YYYY-MM-DD)
  </action>
  <verify>
1. File exists: `lib/data/tutorials.ts`
2. Build succeeds: `npm run build`
3. Tutorial interface exported
4. tutorials array exported with 3 entries
5. All slugs unique: eerste-stappen, configuratie, tips
6. All required fields present in each tutorial
7. All content in Dutch
  </verify>
  <done>
Tutorials data structure exists with type-safe Tutorial interface and array of 3 tutorials with slugs, metadata, and placeholder content structure.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create tutorials overview page</name>
  <files>app/openclaw/tutorials/page.tsx</files>
  <action>
Create `app/openclaw/tutorials/page.tsx` as Server Component:

**Metadata:**
- title: "OpenClaw Tutorials – Leer OpenClaw Gebruiken | AI Assistenten Hub"
- description: "Ontdek onze OpenClaw tutorials: van eerste stappen tot geavanceerde tips. Leer efficiënt werken met je AI-assistent."

**Content structure:**

1. **Page header:**
   - H1: "OpenClaw Tutorials"
   - Intro paragraph (2-3 sentences): "Leer OpenClaw gebruiken met onze praktische tutorials. Van installatie tot geavanceerde technieken, alles wat je nodig hebt om productief te worden met je AI-assistent."

2. **Tutorials grid:**
   - Import tutorials array from `@/lib/data/tutorials`
   - Map over tutorials array to create cards
   - Each card:
     - Title (H2 or H3)
     - Description (paragraph)
     - "Laatst bijgewerkt" date badge
     - Link to `/openclaw/tutorials/[slug]` using Button with asChild pattern
     - Button text: "Lees tutorial" (variant: default)

3. **Card styling:**
   - Grid layout: 1 column mobile, 2 columns tablet (md:grid-cols-2), 3 columns desktop (lg:grid-cols-3)
   - Card appearance: border, rounded, padding, hover effect
   - Date badge: small text, muted color (text-muted-foreground, text-xs or text-sm)
   - Spacing: gap between cards, padding within cards

**Styling rules:**
- Semantic token classes ONLY (bg-background, text-foreground, border-border, etc.)
- Container for responsive layout (max-w-7xl mx-auto px-4)
- Proper spacing (space-y-6 for sections, gap-6 for grid)
- Card hover state: border color change or subtle shadow
- NO raw color classes (text-gray-600, bg-blue-50, etc.)

**Implementation notes:**
- Server Component (no 'use client')
- Import tutorials from @/lib/data/tutorials
- Import Button from @/components/ui/button
- Import Link from next/link
- Use Button with asChild: `<Button asChild><Link href={...}>...</Link></Button>`
- Format date for display (e.g., "9 februari 2026" or "2026-02-09")
  </action>
  <verify>
1. File exists: `app/openclaw/tutorials/page.tsx`
2. Build succeeds: `npm run build`
3. Metadata exports title and description
4. Page renders H1 and intro paragraph
5. Page displays 3 tutorial cards in grid layout
6. Each card shows title, description, date, and button link
7. Links point to correct slugs: /openclaw/tutorials/eerste-stappen, etc.
8. NO raw color classes (grep for text-gray, bg-blue, etc.)
9. All Button components use asChild pattern
  </verify>
  <done>
Tutorials overview page exists at `/openclaw/tutorials` displaying 3 tutorial cards with proper metadata, grid layout, and semantic styling. Each card links to its detail page via slug.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create tutorial detail page with dynamic routing</name>
  <files>app/openclaw/tutorials/[slug]/page.tsx</files>
  <action>
Create `app/openclaw/tutorials/[slug]/page.tsx` as Server Component with dynamic routing:

**Function signature:**
```typescript
// Next.js 15+ params pattern (async)
export default async function TutorialPage({
  params,
}: {
  params: Promise<{ slug: string }>
}) {
  const { slug } = await params
  // ... lookup tutorial by slug
}
```

**generateStaticParams export:**
```typescript
export async function generateStaticParams() {
  return tutorials.map((tutorial) => ({
    slug: tutorial.slug,
  }))
}
```

**generateMetadata export:**
```typescript
export async function generateMetadata({
  params,
}: {
  params: Promise<{ slug: string }>
}): Promise<Metadata> {
  const { slug } = await params
  const tutorial = tutorials.find((t) => t.slug === slug)

  if (!tutorial) {
    return {
      title: 'Tutorial niet gevonden',
    }
  }

  return {
    title: `${tutorial.title} | AI Assistenten Hub`,
    description: tutorial.description,
  }
}
```

**Page logic:**
1. Await params to get slug
2. Find tutorial in tutorials array by slug
3. If not found: redirect to `/openclaw/tutorials` using Next.js `redirect()` or `notFound()`
4. If found: render tutorial content

**Content structure:**

1. **Breadcrumb (optional, nice-to-have):**
   - "OpenClaw > Tutorials > [Tutorial Title]"
   - Links: /openclaw, /openclaw/tutorials, current page

2. **Tutorial header:**
   - H1: tutorial.title
   - Date badge: `Laatst bijgewerkt: ${tutorial.lastUpdated}`
   - Intro paragraph: tutorial.content.intro

3. **Tutorial steps:**
   - For each step in tutorial.content.steps:
     - H2: step.heading
     - Paragraph: step.content

4. **Footer CTA:**
   - Paragraph: "Meer leren? Bekijk onze andere tutorials."
   - Button (variant: secondary): "Terug naar overzicht" → `/openclaw/tutorials`

**Styling:**
- Semantic token classes ONLY
- Container for readability (max-w-3xl mx-auto for prose content)
- Proper spacing between sections (space-y-8 or space-y-12)
- Prose styling: comfortable line height, readable font size
- Date badge: text-sm, text-muted-foreground
- Steps clearly separated (space-y-6)
- Button with asChild pattern for CTA

**Error handling:**
- If slug not found: `redirect('/openclaw/tutorials')` (import from 'next/navigation')
- Alternative: Use `notFound()` to show 404 page
- Prefer redirect for better UX (user lands on working page)

**Implementation notes:**
- Server Component (no 'use client')
- Import tutorials from @/lib/data/tutorials
- Import redirect or notFound from next/navigation
- Import Button and Link
- Support three slugs: eerste-stappen, configuratie, tips
- Metadata per tutorial via generateMetadata
- Static generation via generateStaticParams
  </action>
  <verify>
1. File exists: `app/openclaw/tutorials/[slug]/page.tsx`
2. Build succeeds: `npm run build`
3. generateStaticParams exports 3 static paths
4. generateMetadata generates unique metadata per slug
5. Visit /openclaw/tutorials/eerste-stappen - displays First Steps tutorial
6. Visit /openclaw/tutorials/configuratie - displays Configuration tutorial
7. Visit /openclaw/tutorials/tips - displays Tips & Tricks tutorial
8. Visit /openclaw/tutorials/unknown-slug - redirects to overview or shows 404
9. Each tutorial page shows title, date, intro, steps, and CTA button
10. NO raw color classes
11. All text in Dutch
  </verify>
  <done>
Tutorial detail page exists at `/openclaw/tutorials/[slug]` supporting three tutorials with dynamic routing, proper metadata, and graceful handling of unknown slugs. Each tutorial displays structured content ready for expansion in Phase 19.
  </done>
</task>

</tasks>

<verification>
**Automated checks:**
1. All 3 files exist in correct locations
2. Build passes: `npm run build`
3. No TypeScript errors
4. generateStaticParams generates 3 paths
5. Each tutorial has unique metadata
6. NO raw color classes in any file

**Manual checks:**
1. Visit http://localhost:3000/openclaw/tutorials
   - Overview displays 3 tutorial cards
   - Each card shows title, description, date
   - Click each card - navigates to detail page
2. Visit http://localhost:3000/openclaw/tutorials/eerste-stappen
   - Tutorial title, date, intro, and steps display
   - Content is in Dutch
   - "Terug naar overzicht" button works
3. Visit http://localhost:3000/openclaw/tutorials/configuratie
   - Different content from eerste-stappen
   - All sections render correctly
4. Visit http://localhost:3000/openclaw/tutorials/tips
   - 4 tip steps display (not 3 like others)
5. Visit http://localhost:3000/openclaw/tutorials/invalid-slug
   - Redirects to overview OR shows 404 (either acceptable)
6. Test mobile responsiveness:
   - Overview grid collapses to 1 column
   - Detail page text remains readable
7. Test dark mode:
   - Cards, text, borders remain clear and accessible
</verification>

<success_criteria>
- `/openclaw/tutorials` displays 3 tutorial cards with working links
- `/openclaw/tutorials/[slug]` supports three slugs with unique content
- Unknown slugs handled gracefully (redirect or 404)
- All pages use Dutch content and proper metadata
- All styling uses semantic tokens only
- Structure ready for content expansion in Phase 19
- Build passes clean with no TypeScript errors
- No hardcoded colors or "Coming soon" placeholder text
</success_criteria>

<output>
After completion, create `.planning/phases/10-openclaw-tutorials/10-01-SUMMARY.md`
</output>
